# Rule A implementation — 2026-09-11

- Scope: ball code and regenerated human ledger only; original labels immutable; no inference, training or push; one commit.
- Decision: every visible ball counts for detection; match_ball is a separate tracker flag.
- Done: schema v2 migration, persistent review state and ranked saved-box queue; build 9; scoring switches/provisional tables; all three review notes and throughput guard.
- Done: final worktree and staged-tree git-archive gates passed; implementation ready for the single local commit.
- Next: MJ confirms 188 frames; re-score after review. No new rule-A headline published.
- Master CONTINUITY.md left unchanged because it is outside the explicit write fence.

- Migrated 1,057 rows to `~/codex-runs/ball-human-truth-v2.jsonl`; original untouched.
- Original SHA256: `a0f51b42cb95e5327bf45fd353fd46f50495f99c096709b8836c7309ed2373ff`.
- V2 SHA256: `e535d84d27bc77c0c4d0c24abc8a1cc8653e4e1a005e5e6d494f9e6956e2debf`.
- Queue: 182 old no-ball + n21 s0–s5 = 188; 126 saved-box suggestions, 62 without; 0 confirmed. WASB point outputs cannot supply boxes.
- Kit: 1,105 cached frames, 688 normal suggestions retained; HTML/metadata/queue/migration audit copied to `~/codex-runs/ball-truth-review-build9/` (no JPEGs).
- Browser: isolated Chromium validates seed/no auto-save, Enter/click/N/M/arrows/J/K, normal defaults, v2 export/import with provenance, and legacy storage.
- Scorers: private saved-only smoke reports show `PROVISIONAL: 0 of 188 review frames confirmed` in all four table headers across the human and round-5 reports; no committed scores changed.
- Notes: TRAIN-2 qualifier plus TRAIN-1 tie values; capitalized integrity result; historical trainer same-run observed-hash caveat.
- Guard: checks YOLO, old RF and new RF checkpoints before any model/runtime import or wait; timing fixture unchanged.

- Final gates (worktree AND git archive): `.loan` 472 passed / 1 skipped (Playwright absent); `.venv-mj` 473 passed, including Chromium. Six existing Pillow deprecation warnings in each environment.
- Ruff check and format check: all 110 ball/bench Python files pass in worktree/archive; Python compile and JavaScript syntax checks pass.
- Ledger regeneration from the staged/committed aggregate fixtures is byte-for-byte in both environments (pytest), plus an explicit archive regeneration/cmp. No label, weight, detection, frame or crop added to git.
- Gate logs: `~/codex-runs/ball-any-ball-{worktree,archive}-{loan,rf}.log`; isolated real-kit screenshot: `~/codex-runs/ball-truth-review-build9-check.png`.
- Not done: MJ's review (0/188), laptop sync by orchestrator, final rule-A headline re-score, tracker use of match_ball. No push, training, model inference or throughput rerun performed.

## Build 10 safety fix round

- Now: F1–F4, F6, F7; one commit atop 5222f947; same fences; no push/training/inference. F5 explicitly deferred.
- Both immutable label hashes rechecked unchanged before work.
- Preserved build-8 copy: `~/codex-runs/ball-truth-review-build8/index.html` SHA256 `164ed33753991ef381f706d880936b9190056512add26c9e110e29a5c00e39a7`; build.json `19dc12428158a386ffb06fd4487e671e2bc634bff3a449c8945cea6a1b9fae00`.
- Decision: commit only the original 188 clip|time identities, without label values or pixel coordinates. Queue/provisional membership is independent of row-supplied review_frame flags.
- Decision: v2 storage has timestamped labels and deletion records in one atomic envelope; v1 is read once only. Corrupt storage disables editing until an exported recovery backup and validated, explicitly confirmed re-import.

- F1 browser evidence: exact preserved build-8 page + real original export opened beside build 10 in isolated Chromium. Migrated comparison: 0 missing, 0 extra, 0 changed rows; build-8 N rewrote only v1 and preserved both v2 confirmations. Legacy input unchanged by build 10.
- F1 read-only tests: malformed legacy/v2 storage, invalid v2 label, malformed legacy clear history; all edit keys/click/save blocked; raw recovery export retained the failed value; confirmed validated re-import recovered.
- F2 browser evidence: two pages, event notification, intentionally stale tab memory, distinct confirmations (2 retained), newer same-label update wins, and cross-tab clear remains cleared after another save.
- F3 browser evidence: v1 and v2-unconfirmed imports report counts; cancelling downgrade preserves row/timestamp; explicit confirmation permits replacement; unchanged import reports unchanged.
- F4: queue membership frozen as 188 keys plus pending rows; both non-queue-confirmation and extra-pending-row banner bypasses tested. Value diff CLI ignores formatting; any new input is compared with immutable baseline before regeneration. No fresh laptop export supplied this round.
- F6 browser evidence: C preserved actual n21 s1 point and rf_2x2 acceptance provenance while confirming NOT match; synthetic tests also verify timestamp and pending flags.
- F7: versioned builder rejects shared/existing directories; build 10 points to shared frame files. Preserved build-8 copy and current shared kit index/build hashes remained unchanged.
- Build: `~/ball-truth-review-build10/`; five sync files copied byte-identically to `~/codex-runs/ball-truth-review-build10/`; no JPEGs copied.
- New seed only: `~/codex-runs/ball-human-truth-v2-build10.jsonl`, SHA256 `d74900f6b25b86b7017399966514ae263422e47a3fc7d50d7862ef7a36cea491`. All 1,057 prior v2 rows identical except new `updated_at:0`; original two label hashes unchanged. Queue exactly unchanged: 188 entries / 126 suggestions / 0 confirmed; normal suggestions 688.
- Private execution evidence: `~/codex-runs/ball-build10-real-build8-check.json`, `ball-build10-protected-sha.json`, `ball-build10-generation.log`, `ball-build10-browser.png`. No local browser test uses MJ's profile or writes source labels.
- Done: F1–F4/F6/F7 implementation and private build 10; final worktree/archive gates passed.

- Worktree gates: `.loan` 475 passed / 10 skipped (Playwright unavailable); `.venv-mj` 485 passed including all 10 browser checks. Six pre-existing Pillow deprecation warnings per environment.
- Ledger regeneration remains byte-identical with no committed ledger changes.

- Archive gates: `.loan` 475 passed / 10 skipped (browser dependencies absent); `.venv-mj` 485 passed including every Chromium check. Ruff check/format: all 113 Python files pass. Python compile and all three JavaScript syntax checks pass.
- Real saved-label scoring of the new build-10 seed: 188 pending rows, `PROVISIONAL: 0 of 188 review frames confirmed` in all three human-score table headers. No headline or timing changes.
- Gate logs: `~/codex-runs/ball-build10-{worktree,archive}-{loan,rf}.log`; archive path recorded in `ball-build10-archive-path.txt`. Both ledgers regenerate byte-for-byte from committed fixtures.
- Not done: F5 multi-ball schema (explicitly pending MJ), fresh laptop export comparison when supplied, laptop sync, MJ review and final rule-A headline re-score. No push, training, model inference or throughput rerun.

## Build 11 import and merge safety fix round

- Now: P1/P2/N1/N4/N5; one commit atop 70dfad17; same fences; no push/training/inference.
- Hard compatibility: unchanged label schema and v2 envelope/key. Legacy SHA256 uses a separate metadata key; conflict counter is page-local. Existing v2 rows and timestamps must not change on open.
- N1 limitation: build 10 never recorded a legacy hash. An existing build-10 browser establishes its first legacy baseline on its first build-11 open; edits before that baseline cannot be retrospectively detected. Subsequent opens/legacy storage events compare hashes without merging v1.
- Protected source files and all files in shared/build-8/9/10 directories hashed before work; audit is `~/codex-runs/ball-build11-protected-sha.json`. Master CONTINUITY.md remains outside the write fence.
- P1: import prompts for every changed confirmed decision, including incoming confirmed rows. Counts identify confirmed replacements and newer-local decisions; older changed rows are marked STALE. Cancel writes nothing; explicit approval replaces and restamps. Chromium covers coordinates, visibility, match identity and provenance at older/equal/newer timestamps (12 cases), plus prior downgrade coverage.
- P2: explicit tie ranking is confirmed > clear > unconfirmed. Differing confirmed ties retain already-stored values and increment a visible, deduplicated page-local conflict counter. Thirty unit cases cover all equal/newer timestamp pairings and both confirmed conflict directions. Chromium forces equal timestamps and verifies confirmation retention, stored-value retention and the counter.
- N1: SHA256 sidecar detects later legacy changes without another merge. Chromium verifies the warning on reopen; the private harness also uses the preserved actual build-8 N handler to trigger it, with byte-identical v2 afterward.
- N4: README explicitly discloses that 182 of the 188 committed identities reveal MJ's original no-ball frames. Registry remains unchanged for determinism; no new private labels or pixel coordinates committed.
- N5: README and in-page help require current browser export plus value comparison before sync. Existing v2 ignores embedded seed; a fresh export must be explicitly imported in an existing browser. Chromium replaces the embedded seed and proves stored rows/envelope remain identical.
- Compatibility: actual preserved build-10 page made three browser-only confirmations and one clear; build 11 retained all 1,056 remaining rows, timestamps, provenance and clear history: 0 missing / 0 extra / 0 changed. Same v2 key and byte-identical stored envelope. Audit: `~/codex-runs/ball-build11-build10-compatibility.json`. Browser contexts isolated from MJ's profile.
- Build: `~/ball-truth-review-build11/`, five byte-identical sync files in `~/codex-runs/ball-truth-review-build11/`. Frames reused at `../ball-truth-review/`; no JPEGs copied. Queue/saved suggestions byte-identical to build 10: 188 frames, 126 review suggestions, 688 normal suggestions.
- New seed only: `~/codex-runs/ball-human-truth-v2-build11.jsonl`, SHA256 `d74900f6b25b86b7017399966514ae263422e47a3fc7d50d7862ef7a36cea491`. All 1,131 protected files, including both immutable labels and every prior kit file, rehashed unchanged. Generation audit: `~/codex-runs/ball-build11-generation.log`.
- Worktree gates: `.loan` 505 passed / 25 skipped (Playwright unavailable); `.venv-mj` 530 passed including all Chromium cases. Six existing Pillow warnings per environment. Ruff check and format check pass for 115 ball/bench Python files; JavaScript syntax checks pass. Both human ledgers regenerated byte-for-byte in both environments.
- Git archive gates: `.loan` 505 passed / 25 skipped; `.venv-mj` 530 passed, including Chromium. Ruff check/format and JavaScript syntax pass; both ledgers regenerate byte-for-byte in both environments. Archive path/tree recorded in `~/codex-runs/ball-build11-archive-{path,tree}.txt`; logs are `ball-build11-{worktree,archive}-{loan,rf}.log`. Only this execution-note addition followed archive testing; implementation, fixtures and tests are unchanged.
- Done: all five fixes, versioned build 11, compatibility proof and worktree/archive gates. Ready for the requested single local commit atop 70dfad17.
- Not done: laptop sync and mandatory fresh laptop export comparison (no fresh export supplied), MJ review, final rule-A headline re-score, F5 multi-ball schema. No push, merge, training, model inference or throughput rerun.

## Build 12 cleared-label and import safety round

- Scope: items 1–6 atop e5b1b2af; same ball-only fence, one commit, no push/training/inference. MJ has opened neither build 10 nor 11 on his laptop.
- Protected snapshot: `~/codex-runs/ball-build12-protected-sha.json` (1,157 files). Reviewer harnesses read and executed from copies under `~/codex-runs/ball-build12-reviewer-repro/`, with output paths redirected so the protected review directory is never written. Master CONTINUITY.md remains outside the fence.
- RED-first: added `test_build12_safety.py` before implementation edits; baseline run logged at `~/codex-runs/ball-build12-red.log`. Fixes must preserve v2 label/envelope schema and key; no private label data added to fixtures.
- RED evidence on e5b1b2af implementation: all 13 new cases failed (six clear-import cases, mixed stale import, bounded stale list, full-export acknowledgement, confirmed dismissal, missing WebCrypto, semantic comparison, and missing frozen-build10 fixture). Baseline source modules were unchanged during that run. Reviewer real script reproduced 3/3 confirmations lost after OK; synthetic harness reproduced restored clears and insecure-HTTP read-only. Its older real subcase timed out after a no-change N; the separately supplied guaranteed-changing real script completed and reproduced the defect.
- GREEN item 1: clear timestamps participate in max local decision time. Older/equal imports cannot restore normal or confirmed cleared rows, including in fresh tabs and exports; strictly newer restoration requires approval and is never counted as new.
- GREEN item 2: stale rows are filtered before prompts and cannot be approved. Mixed import applies eligible rows only; skipped count and first 20 keys plus remaining count are tested. Existing stale-backup/downgrade tests now require preservation. Approval tests still cover equal/newer changed confirmed decisions and byte-identical Cancel. Imports also reject a storage change during an approval dialog.
- GREEN item 3: matching complete legacy exports acknowledge all current legacy rows, including skipped stale rows. Partial imports keep the warning. Confirmed manual dismissal re-baselines; reload/new-tab persistence and later legacy edits are tested.
- GREEN item 4: comparison invokes committed migration, uses three semantic/metadata categories, reports keys/field names without coordinates, exits 0 for equivalent labels/review state and 1 for differences. Synthetic migration/zero-edit/moved-click/confirmation cases pass.
- GREEN item 5: tagged pure-JS FNV-1a 64-bit change detector replaces WebCrypto; unknown/missing tags silently re-baseline. Chromium with crypto.subtle removed remains editable. Actual HTTP at 192.168.86.97 reports secure=false, subtle=false, readOnly=false and saves an edit.
- GREEN item 6: frozen JS modules are byte-identical to 70dfad17 and run against synthetic rows in CI before opening the current page. Equal-time same-kind comparisons now use different rows, including stored confirmed conflict retention. All 30 merge unit cases pass.
- Private build-10 AND build-11 compatibility: snapshots captured before opening build 12 compare 1,057 initial rows and 1,056 rows after edits/clear, each 0 missing / 0 extra / 0 changed, same key and byte-identical stored envelope including tombstones. Audit: `~/codex-runs/ball-build12-compatibility.json`.
- Real fix-path: build-8 click, bootstrap, 3 confirmations, another build-8 click and full export/import yields exactly 1 changed click, 3 skipped newer reviews, 1,053 unchanged rows, 0 prompts (no eligible protected change), and all 3 confirmations unchanged. Warning hides after import/reload/new tab, returns on next legacy click, remains after partial import.
- Real comparison: `ball-human-truth.jsonl` versus build-12 seed = LABEL 0 / REVIEW-STATE 0 / METADATA-only 1,057, verdict same, exit 0. Metadata differences are expected migration fields/schema/timestamps. Actual zero-edit browser export versus seed = 0 / 0 / 0. Audit: `~/codex-runs/ball-build12-real-compare.json`.
- Build: `~/ball-truth-review-build12/`; five byte-identical files at `~/codex-runs/ball-truth-review-build12/`; shared frames reused via `../ball-truth-review/`. Queue and saved suggestions byte-identical to build 11 (188 review frames, 126 suggestions; 688 normal suggestions). New seed only: `~/codex-runs/ball-human-truth-v2-build12.jsonl`, SHA256 `d74900f6b25b86b7017399966514ae263422e47a3fc7d50d7862ef7a36cea491`.
- Worktree gates: `.loan` 506 passed / 37 browser skips; `.venv-mj` 543 passed / 0 skipped. Six existing Pillow warnings in each. Ruff check/format pass (117 Python files); all current JavaScript syntax checks pass. Both ledgers regenerate byte-for-byte in both environments. All 1,157 protected file hashes unchanged; original v1/v2/build10 seed hashes remain a0f51b42/e535d84d/d74900f6.
- Not done: push, PR merge, laptop sync and mandatory fresh laptop export/comparison, MJ review, final rule-A headline re-score, deferred multi-ball schema. No training, inference or throughput rerun. All browser work used isolated contexts, never MJ's profile.
- Final review tightened legacy acknowledgement: baseline exactly the matched v1 value, then re-check current v1, so an intervening old-page edit cannot be silently acknowledged. Only this round's unreleased build-12 HTML/sync copy was refreshed; protected kits remain unchanged. Final gates rerun against this source.
- Archive caught a test-harness interpreter leak: the CLI subprocess used PATH's `python3` (3.9 in the archive) instead of the gate venv. Changed it to `sys.executable`; tests now verify CLI exit codes in the requested Python environment. No application or private-build change. Old comparator on the real migration reports 1,057 changed rows (`ball-build12-compare-red.json`), versus new comparator's 0 LABEL / 0 REVIEW-STATE.
- Final worktree AND git-archive gates: `.loan` 506 passed / 37 skipped (browser dependency absent); `.venv-mj` 543 passed / 0 skipped, including Chromium and frozen-build10 compatibility. Ruff check/format (117 files), JavaScript syntax and ledger byte regeneration pass. Six existing Pillow deprecation warnings in each suite. Gate logs: `~/codex-runs/ball-build12-{worktree,archive}-{loan,rf}.log`; final archive path/tree recorded in `ball-build12-archive-{path,tree}.txt`. Only this execution-note update followed the verified archive; implementation and tests are identical.
- Done: items 1–6, RED/GREEN evidence, private build/audit, semantic real-file comparisons, protected-hash verification and all gates. Ready for the requested single local commit atop e5b1b2af.

## Build 13 explicit reconciliation and recovery round

- Scope: items 1–5 atop c986c8c2; one local commit, ball-only fence, no push/training/inference. MJ has opened none of builds 10/11/12.
- Protected snapshot: `~/codex-runs/ball-build13-protected-sha.json` (1,184 files). Reviewer scripts/results/log read; scripts run only from copies under `~/codex-runs/ball-build13-reviewer-repro/`, with output paths redirected. Protected review/kit directories untouched. CONTINUITY.md remains outside the write fence.
- RED-first regression suite written before implementation edits: `test_build13_safety.py`; baseline log `~/codex-runs/ball-build13-red.log`. Raw legacy baselines and per-key choices will be separate browser metadata; v2 label/envelope/key remain compatible.
- RED evidence: all 14 initial cases fail on c986c8c2 (H1 Use/Keep, H2a/H2b/H3, SHA/FNV matching/changed hash upgrades, full pending list/Keep/bulk/quota, recovery, clear tie, comparison categories, wording/binding). Additional focused RED cases exposed stale-tab automatic resolution and hash-fallback clearing on v1 reversion; both fixed and pinned. Logs: `ball-build13-red.log`, `ball-build13-stale-tab-red.log`, `ball-build13-fallback-latch-red.log` under `~/codex-runs/`.
- Item 1 GREEN: raw baseline + taken_at + per-key Keep choices; visible/x/y tolerance 1e-6; additions/changes/drops reconciled directly from old-page storage. Full scrollable jump list, values, Use/Keep, confirmed bulk Use and count-aware Dismiss. Imports clear no warning unless data actually matches. Hash-history fallback stays latched until Dismiss, even if v1 reverts.
- Metadata decision: new `:legacy-baseline-v1` key reads/fixes the prior hash once without overwriting `:legacy-sha256`; this prevents build-12 metadata rewrite loops. Matching old SHA/FNV upgrades silently; changed/unknown hashes persist the fallback. Raw quota failure uses hash-only mode and never read-only. Reconciliation refreshes stored labels before resolving, so suspended tab memory cannot hide a pending decision.
- Item 2 GREEN: recovery preserves last validated memory instead of emptying it; accepts own backup with validated labels and clear history, plus JSONL. Fresh-context recovery-backup import tested. Real private A/B: preserved build 12 loses frame 188 (188 rows); build 13 retains it (189 rows) including a fresh tab, and restores own backup labels/clears.
- Item 3 GREEN: shared compareDecision makes clear beat confirmed/unconfirmed edits at equal time in import and merge. Row-row import accepts a non-stale equal row; merge retains storage with confirmation priority. Different-row equal-time pair matrix updated and passes.
- Item 4 GREEN: review_frame is REVIEW-STATE; changed unknown fields are UNKNOWN-FIELD and force differs/exit 1. Keys and field names only, no coordinate values. Reviewer comparison probe rerun against fixed code.
- Item 5 GREEN: assert/fixture docs say the test suite, README states GitHub CI does not run spike tests and browser checks run only in local .venv-mj. Current and prior build-12 private harnesses bind LAN only. Protected build 11 needs localhost's secure origin; the current harness forwards that browser origin to the single LAN listener without changing old code or binding loopback/tailnet. Plain LAN HTTP is checked separately without forwarding.
- Real private compatibility: build 10, 11 AND 12 -> 13 each retain 1,057 initial rows and 1,056 after edits/clear: 0 missing / 0 extra / 0 changed; v2 stored value byte-identical, clear history retained. Audit: `~/codex-runs/ball-build13-compatibility.json`.
- Real H1 Use and Keep each preserve the warning after stale import, resolve across reload/new tab, and flag a later old-page change. H2a current export, H2b pre-clear export and H3 own export all retain warning and label until Use clears locally. Original real sequence applies 1 new click while preserving 3 reviews; warning hides only once the one changed old-page value matches.
- Measured real raw baseline: 162,392 UTF-8 bytes for raw v1; 180,432 UTF-8 bytes / 360,864 UTF-16 bytes for the baseline envelope. Quota fallback tested in Chromium. Actual LAN HTTP has secure=false, subtle=false, readOnly=false and saves an edit.
- Build: `~/ball-truth-review-build13/`; five identical sync files in `~/codex-runs/ball-truth-review-build13/`. Shared frames at `../ball-truth-review/`; no frame copies. Queue/suggestions identical to build 12: 188 keys, 126 review suggestions, 688 normal suggestions. New seed: `~/codex-runs/ball-human-truth-v2-build13.jsonl`; unchanged migration SHA256 `d74900f6b25b86b7017399966514ae263422e47a3fc7d50d7862ef7a36cea491`.
- All 1,184 protected file hashes verified unchanged; original label hashes a0f51b42/e535d84d/d74900f6 retained. Frozen build-10 JS byte-identical to 70dfad17 (only its README wording corrected). Current label schema/ledger headlines unchanged.
- Not done: push/merge, laptop sync and mandatory fresh laptop export/comparison, MJ review, final rule-A re-score, deferred multi-ball schema. No training/inference or throughput rerun. All browser contexts isolated from MJ's profile.
- Final worktree AND git-archive gates: `.loan` 508 passed / 50 browser skips; `.venv-mj` 558 passed / 0 skipped, including all Chromium checks. Six existing Pillow deprecation warnings per suite. Ruff check/format pass (119 Python files); all JavaScript syntax checks pass. Both human ledgers regenerate byte-for-byte in both environments, with no headline changes.
- Gate logs: `~/codex-runs/ball-build13-{worktree,archive}-{loan,rf}.log`; archive path/tree in `ball-build13-archive-{path,tree}.txt`. Final private audit's page hash matches the generated/sync HTML. Only this execution-note update follows the tested archive; implementation and tests are identical.
- Real original export versus build-13 seed: LABEL 0 / REVIEW-STATE 0 / UNKNOWN-FIELD 0 / METADATA-only 1,057, same/exit 0; actual zero-edit browser export versus seed has all counts 0. No private label values or pixel coordinates added to git.
- Done: items 1–5, baseline RED and final GREEN evidence, versioned build 13, real compatibility/reconciliation/recovery/LAN audit, protected-hash verification and all gates. Ready for the requested single commit atop c986c8c2.
