# Phase 0 video-analysis spike — pinned 2026-06-10 (latest PyPI at pin time).
# License posture: every model/runtime here is Apache-2.0 / MIT / BSD.
# NO ultralytics (AGPL) anywhere — do not add it, even transitively on purpose.
#
# If pip's resolver complains (rfdetr pins its own torch/transformers ranges),
# relax the torch/transformers/numpy pins to whatever rfdetr 1.7.1 requires —
# the spike does not depend on exact minor versions of those.

torch==2.12.0
rfdetr==1.7.1               # Apache-2.0 (Nano–Large only; XL/2XL are PML-licensed — never used here)
supervision==0.28.0         # MIT — InferenceSlicer, annotators, ByteTrack fallback
trackers==2.4.0             # Apache-2.0 — BoTSORTTracker (camera-motion compensation)
transformers==5.10.2        # Apache-2.0 — SiglipVisionModel + AutoImageProcessor
sentencepiece==0.2.1        # only needed if something loads the SigLIP text tokenizer
umap-learn==0.5.12          # BSD — 3D projection before KMeans (roboflow/sports recipe)
scikit-learn==1.9.0         # BSD — KMeans, silhouette score
opencv-python-headless==4.13.0.92  # Apache-2.0 — VideoWriter for the annotated sample clip
av==17.1.0                  # PyAV (BSD wrapper; ffmpeg libs LGPL) — decode + fps sampling
numpy==2.4.6                # if umap-learn/numba reject this, drop to numpy~=2.2

# footage download tooling (download_footage.py only — not needed on the GPU box
# if you fetch footage elsewhere and scp it over)
gdown==5.2.0                # SoccerTrack v2 Drive mirror (CC BY 4.0)
yt-dlp==2026.6.9            # CC-licensed footage only — script enforces per-video license check
