# Task brief — P0-A11: stale thread loads, paged request lists, moot consent controls, fixed test (review follow-ups)

**Pattern:** copy-adapt · **Thinking:** off · **Budget:** 30 min ·
**Files you will touch (ALL replaced by shipped files — eight `cp`s, nothing to type):**
`academy-watch-frontend/src/lib/introductions.js`, `academy-watch-frontend/src/components/contact/ContactThread.jsx`,
`academy-watch-frontend/src/components/contact/ClubIntroductionsPanel.jsx`, `academy-watch-frontend/src/pages/IntroductionsPage.jsx`,
`academy-watch-frontend/tests/introduce-dialog.test.mjs`, `academy-watch-frontend/tests/contact-thread.test.mjs`,
`academy-watch-frontend/tests/club-introductions-panel.test.mjs`, `academy-watch-frontend/tests/introductions-page.test.mjs`. Nothing else.

## The situation

Second review round (codex) on the contact UI: (1) P1 — selecting two introductions quickly could let the slower, older
thread load overwrite the newer one (wrong conversation under the header); (2) P1 — a test still asserted the
pre-flag `{player.contactable ? (` condition; (3) P2 — the club panel showed Allow/Decline for a pending consent on a
request that was already withdrawn/declined/expired (the API rejects it); (4) P2 — both request lists fetched only the
first 100 rows. The shipped files sequence thread loads and discard stale results, gate consent controls on
`canDecideConsent` (pending consent AND an active request), page through the lists with `fetchAllRequests`, and
update the tests.

## Step 0 — replace the eight files (two commands)

```bash
cp briefs/assets/P0-A11/introductions.js academy-watch-frontend/src/lib/introductions.js && cp briefs/assets/P0-A11/ContactThread.jsx academy-watch-frontend/src/components/contact/ContactThread.jsx && cp briefs/assets/P0-A11/ClubIntroductionsPanel.jsx academy-watch-frontend/src/components/contact/ClubIntroductionsPanel.jsx && cp briefs/assets/P0-A11/IntroductionsPage.jsx academy-watch-frontend/src/pages/IntroductionsPage.jsx && echo SOURCES-REPLACED
```

```bash
cp briefs/assets/P0-A11/introduce-dialog.test.mjs academy-watch-frontend/tests/introduce-dialog.test.mjs && cp briefs/assets/P0-A11/contact-thread.test.mjs academy-watch-frontend/tests/contact-thread.test.mjs && cp briefs/assets/P0-A11/club-introductions-panel.test.mjs academy-watch-frontend/tests/club-introductions-panel.test.mjs && cp briefs/assets/P0-A11/introductions-page.test.mjs academy-watch-frontend/tests/introductions-page.test.mjs && echo TESTS-REPLACED
```

Confirm, read-only: `grep -c "fetchAllRequests" academy-watch-frontend/src/lib/introductions.js academy-watch-frontend/src/components/contact/ClubIntroductionsPanel.jsx academy-watch-frontend/src/pages/IntroductionsPage.jsx` → `…introductions.js:2`, `…ClubIntroductionsPanel.jsx:2`, `…IntroductionsPage.jsx:2`.

## Step 1 — gate

```bash
make gate TASK=P0-A11
```

GREEN (node --test ×4, then `pnpm lint` 0 errors + `pnpm build`; 1–4 minutes).

## When things go wrong

- `pnpm lint` error in a replaced file → STOP, say BLOCKED, paste it (the files are shipped; if they lint dirty, the brief is wrong, not you).
- Same error twice → STOP, BLOCKED.

## Do not

- Do not edit any of the eight files by hand, or touch anything else.

## Done means

1. `make gate TASK=P0-A11` green — you ran it, you saw it.
2. The confirm grep prints `2`, `2`, `2`.
3. Handback on disk at `.harness/handback/$HARNESS_SESSION.md` + the last line `HANDBACK-FILED: .harness/handback/$HARNESS_SESSION.md`.
