# Task brief — P0-B2: the club console reads its match list from the backend

**Pattern:** copy-adapt · **Thinking:** off · **Budget:** 60 min ·
**Files you will touch:** `academy-watch-frontend/src/lib/api.js` (ONE new method),
`academy-watch-frontend/src/pages/MyClubConsole.jsx` (four small edits named below), and
`academy-watch-frontend/tests/club-console-match-list.test.mjs` (NEW). Nothing else.
**Depends on:** P0-B1 (the endpoint `GET /api/club/<program_id>/matches`) — already landed on this branch.

**Shipped files:** this brief ships its NEW files under `briefs/assets/P0-B2/`; each numbered section below gives the exact `cp` command. Never retype a shipped file — copy it. Your work is the edits inside existing files and the gate.

## The situation

The club console remembers match ids in the browser's localStorage and reconstructs the list with one
GET per id, because the backend had no list endpoint. It has one now (`GET /api/club/<id>/matches` →
`{ matches: [...], total }`, newest first, each row shaped like `getClubMatch` minus `roster`). Switch the
console to it and delete the localStorage index entirely, so the history follows the manager to any device.

## The job

### 1. `academy-watch-frontend/src/lib/api.js` — one new method

Find, verbatim (around line 1330):

```js
    static async getClubMatch(programId, matchId) {
        return this.request(`/club/${encodeURIComponent(programId)}/matches/${encodeURIComponent(matchId)}`)
    }
```

Insert immediately ABOVE it:

```js
    static async listClubMatches(programId) {
        return this.request(`/club/${encodeURIComponent(programId)}/matches`)
    }

```

### 2. `academy-watch-frontend/src/pages/MyClubConsole.jsx` — four edits

**2a. Delete the localStorage index.** Line 54 is `const MATCH_INDEX_VERSION = 'v1'` — delete it. Lines
111-131 are the three helpers, verbatim:

```js
function matchIndexKey(programId) {
  return `club-console:matches:${MATCH_INDEX_VERSION}:${programId}`
}

function loadMatchIds(programId) {
  try {
    const value = JSON.parse(localStorage.getItem(matchIndexKey(programId)) || '[]')
    if (!Array.isArray(value)) return []
    return [...new Set(value.filter((id) => Number.isInteger(id) && id > 0))].slice(0, 100)
  } catch {
    return []
  }
}

function saveMatchIds(programId, ids) {
  try {
    localStorage.setItem(matchIndexKey(programId), JSON.stringify([...new Set(ids)].slice(0, 100)))
  } catch {
    // The console remains usable when storage is disabled; the local match index
    // simply cannot survive a reload because C2 exposes no list endpoint.
  }
}
```

Delete all three functions (and the blank line between them).

**2b. The `created` callback** (around line 946) is, verbatim:

```js
  const created = (response) => {
    if (!response?.id) return
    const { upload: grant, ...match } = response
    upsertMatch({ ...match, roster: [], processing_request_status: null })
    if (grant) onUploadGrantChange(match.id, grant)
    const ids = [match.id, ...loadMatchIds(programId).filter((id) => id !== match.id)]
    saveMatchIds(programId, ids)
    setSelectedId(match.id)
  }
```

Delete the two lines `const ids = …` and `saveMatchIds(programId, ids)` so it reads:

```js
  const created = (response) => {
    if (!response?.id) return
    const { upload: grant, ...match } = response
    upsertMatch({ ...match, roster: [], processing_request_status: null })
    if (grant) onUploadGrantChange(match.id, grant)
    setSelectedId(match.id)
  }
```

**2c. The empty-state copy** (around line 990) is, verbatim:

```jsx
        <EmptyState icon={Film} title="No matches in this browser yet">Create the first match workspace. Until a backend list endpoint exists, this browser remembers the match IDs it creates.</EmptyState>
```

Replace with:

```jsx
        <EmptyState icon={Film} title="No matches yet">Create the first match workspace. Your club's matches are saved to your account and follow you to any device.</EmptyState>
```

**2d. `loadMatches`** (around line 1100) is, verbatim:

```js
  const loadMatches = useCallback(async () => {
    setMatchesLoading(true)
    setMatchesError(null)
    setMatchesLoadFailureCount(0)
    const ids = loadMatchIds(programId)
    if (ids.length === 0) {
      setMatches([])
      setMatchesLoading(false)
      return
    }
    const results = await Promise.allSettled(ids.map((id) => APIService.getClubMatch(programId, id)))
    if (!mountedRef.current) return
    const denied = results.some((result) => result.status === 'rejected' && result.reason?.status === 403)
    if (denied) {
      onAccessDenied()
      return
    }
    const loaded = results.flatMap((result) => result.status === 'fulfilled' ? [result.value] : [])
    const retainedIds = results.flatMap((result, index) => result.status === 'fulfilled' || result.reason?.status !== 404 ? [ids[index]] : [])
    const failureCount = results.filter((result) => result.status === 'rejected' && result.reason?.status !== 404).length
    saveMatchIds(programId, retainedIds)
    setMatches(loaded)
    setMatchesLoadFailureCount(failureCount)
    if (loaded.length === 0 && results.some((result) => result.status === 'rejected' && result.reason?.status !== 404)) {
      setMatchesError('Saved match details could not be loaded. Try again.')
    }
    setMatchesLoading(false)
  }, [onAccessDenied, programId])
```

Replace the WHOLE function with:

```js
  const loadMatches = useCallback(async () => {
    setMatchesLoading(true)
    setMatchesError(null)
    setMatchesLoadFailureCount(0)
    try {
      const response = await APIService.listClubMatches(programId)
      if (!mountedRef.current) return
      setMatches(Array.isArray(response?.matches) ? response.matches : [])
    } catch (error) {
      if (!mountedRef.current) return
      if (error?.status === 403) {
        onAccessDenied()
        return
      }
      setMatches([])
      setMatchesLoadFailureCount(1)
      setMatchesError(errorText(error, 'Matches could not be loaded. Try again.'))
    } finally {
      if (mountedRef.current) setMatchesLoading(false)
    }
  }, [onAccessDenied, programId])
```

`errorText(error, fallback)` already exists in this file (line ~86); `mountedRef`, `onAccessDenied`,
`setMatches…` are the same state setters the old function used. The rows from the list endpoint carry
`job` and `processing_request_status` but no `roster`; the existing detail view fetches the full match
(`refreshSelected` → `getClubMatch`) when a match is selected, exactly as before.

## The test — create `academy-watch-frontend/tests/club-console-match-list.test.mjs` (write it FIRST)

Shipped with this brief — COPY it, never retype (one command):

```bash
mkdir -p academy-watch-frontend/tests && cp briefs/assets/P0-B2/club-console-match-list.test.mjs academy-watch-frontend/tests/club-console-match-list.test.mjs
```

## How to start

1. `PLAN.md`, at most 10 lines. Then act.
2. Copy the test file (the `cp` command above). Run `make gate TASK=P0-B2`. RED on the first assertion. Correct.
3. Edit `api.js` (1), then the four console edits (2a-2d), one at a time. Gate again. GREEN (this gate
   runs `pnpm lint` + `pnpm build`, about 70 seconds).

## When things go wrong

- `pnpm lint`: `'loadMatchIds' is not defined` → you deleted the helpers but left a caller; the only
  callers were in `created` (2b) and `loadMatches` (2d). Fix those.
- `pnpm lint`: `'MATCH_INDEX_VERSION' is assigned a value but never used` → delete line 54 (2a).
- `pnpm build` fails with a JSX error near the EmptyState → the replacement in 2c must keep the exact
  `<EmptyState icon={Film} title="…">…</EmptyState>` shape.
- Same error twice → STOP, BLOCKED, paste it.
- After ANY interruption: run the gate; whatever is red is your next step.

## Do not

- Do not touch the roster panel, upload flow, `refreshSelected`, `upsertMatch`, or any other page.
  Do not add caching or pagination.

## Done means

1. `make gate TASK=P0-B2` green — you ran it, you saw it.
2. `grep -c "localStorage" academy-watch-frontend/src/pages/MyClubConsole.jsx` prints `0`.
3. Handback file on disk + the `HANDBACK-FILED: .harness/handback/$HARNESS_SESSION.md` last line.
