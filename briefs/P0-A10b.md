# Task brief — P0-A10b: public `/api/features` flag endpoint (review follow-up)

**Pattern:** copy-adapt · **Thinking:** off · **Budget:** 30 min ·
**Files you will touch:** `academy-watch-backend/src/routes/auth_routes.py` (one shipped route appended by a step script)
and `academy-watch-backend/tests/test_features_route.py` (NEW, shipped). Nothing else.

## Shipped step scripts

`bash briefs/assets/P0-A10b/step-1.sh` is the brief's own command shipped as a file. Run it EXACTLY like that, from the
worktree root. Never open, copy out, or retype it. It prints `ROUTE-APPENDED` and the route's line number; `STEP1-BLOCKED`
means STOP and say BLOCKED with the line.

## The situation

PR review (codex, P1) on the contact UI: the web app shows "Introductions" entry points even when the backend's
`CONTACT_RAIL_ENABLED` flag is off (then every `/api/contact/*` route answers 404). The client needs to know the flag.
This task adds one tiny public endpoint, `GET /api/features` → `{"contact_rail": true|false}`, read from the existing
`contact_rail_enabled()` helper. The frontend gating is the next task (P0-A10).

## Step 0 — the test first (one command)

```bash
cp briefs/assets/P0-A10b/test_features_route.py academy-watch-backend/tests/test_features_route.py && echo TEST-COPIED
```

Run `make gate TASK=P0-A10b` once now: RED (`404` for `/api/features`). Correct.

## Step 1 — append the route (one command)

```bash
bash briefs/assets/P0-A10b/step-1.sh
```

Prints `ROUTE-APPENDED` then `route at line <n>`.

## Step 2 — gate

```bash
make gate TASK=P0-A10b
```

GREEN (ruff check + ruff format --check + the new test; ~20 s).

## When things go wrong

- `STEP1-BLOCKED` → STOP, BLOCKED, paste the line.
- ruff format complains about `auth_routes.py` → STOP, BLOCKED, paste it (the snippet is shipped; if it formats dirty, the brief is wrong, not you).
- Same error twice → STOP, BLOCKED.

## Do not

- Do not edit `auth_routes.py` by hand, touch `services/contact.py`, or anything else.

## Done means

1. `make gate TASK=P0-A10b` green — you ran it, you saw it.
2. `grep -c "def features" academy-watch-backend/src/routes/auth_routes.py` prints `1`.
3. Handback on disk at `.harness/handback/$HARNESS_SESSION.md` + the last line `HANDBACK-FILED: .harness/handback/$HARNESS_SESSION.md`.
