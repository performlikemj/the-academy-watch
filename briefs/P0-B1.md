# Task brief — P0-B1: backend `GET /api/club/<program_id>/matches` (club match list)

**Pattern:** copy-adapt · **Thinking:** off · **Budget:** 60 min ·
**Files you will touch:** `academy-watch-backend/src/routes/club.py` (ONE new route) and
`academy-watch-backend/tests/test_club_console.py` (ONE line inserted + ONE new test). Nothing else.

## The situation

The club console can create matches (`POST /club/<id>/matches`) and read one
(`GET /club/<id>/matches/<match_id>`) but cannot LIST them. So the web console keeps match ids in the
browser's localStorage, and a manager on a second device sees an empty history. Add the list endpoint.
(Switching the web console to it is a later task.)

## The job

Add this route to `academy-watch-backend/src/routes/club.py`, immediately AFTER `create_club_match`
ends — its last lines are:

```python
    except ValueError as exc:
        db.session.rollback()
        return _bad_request(str(exc))
```

— and BEFORE the next decorator
`@club_bp.route("/club/<int:program_id>/matches/<int:match_id>/sas", methods=["POST"])`.
Keep two blank lines above and below, like every other route. Verbatim:

```python
@club_bp.route("/club/<int:program_id>/matches", methods=["GET"])
@require_club_manager()
def list_club_matches(program_id: int):
    """List one program's matches, newest first. Quota caps a program at MAX_MATCH_QUOTA rows, so no paging."""
    rows = (
        VideoMatch.query.filter_by(club_program_id=program_id)
        .order_by(VideoMatch.created_at.desc(), VideoMatch.id.desc())
        .all()
    )
    matches = []
    for match in rows:
        out = match.to_dict(include_job=True)
        out["processing_request_status"] = "requested" if match.processing_requested_at else None
        matches.append(out)
    return jsonify({"matches": matches, "total": len(matches)})
```

Everything it uses already exists in this file: `club_bp`, `require_club_manager`, `VideoMatch`,
`jsonify`. The per-match shape copies `get_club_match` (the existing route at line ~438), minus its
`roster` list (too heavy for a list). For reference, that existing route is:

```python
@club_bp.route("/club/<int:program_id>/matches/<int:match_id>", methods=["GET"])
@require_club_manager()
def get_club_match(program_id: int, match_id: int):
    match = _club_match(program_id, match_id)
    if match is None:
        return jsonify({"error": "Match not found"}), 404
    out = match.to_dict(include_job=True)
    out["roster"] = [entry.to_dict() for entry in match.roster_entries]
    out["processing_request_status"] = "requested" if match.processing_requested_at else None
    return jsonify(out)
```

## The tests — write them FIRST

Both edits are in `academy-watch-backend/tests/test_club_console.py`.

1. In `test_every_club_console_route_is_manager_gated_before_resource_access` (line 288) the
   `attacks` tuple starts:

   ```python
       attacks = (
           ("GET", f"/api/club/{b}/roster", None),
           ("POST", f"/api/club/{b}/roster", {"player_api_id": 7001}),
   ```

   Insert this line right after the `("GET", f"/api/club/{b}/roster", None),` line:

   ```python
           ("GET", f"/api/club/{b}/matches", None),
   ```

2. Add this new test at the very END of the file (after the last test function):

   ```python
   def test_list_club_matches_is_program_scoped_and_newest_first(club_app, client):
       a = club_app.c2["program_a"]
       b = club_app.c2["program_b"]
       first = _match(a, status="uploaded")
       second = _match(a, status="finalized")
       other = _match(b, status="uploaded")

       response = client.get(f"/api/club/{a}/matches", headers=_headers("a"))
       assert response.status_code == 200, response.get_json()
       body = response.get_json()
       assert body["total"] == 2
       assert [row["id"] for row in body["matches"]] == [second.id, first.id]
       assert body["matches"][0]["status"] == "finalized"
       assert body["matches"][0]["processing_request_status"] is None
       assert "job" in body["matches"][0]
       assert "roster" not in body["matches"][0]
       assert other.id not in [row["id"] for row in body["matches"]]

       response_b = client.get(f"/api/club/{b}/matches", headers=_headers("b"))
       assert response_b.status_code == 200
       assert [row["id"] for row in response_b.get_json()["matches"]] == [other.id]

       assert client.get(f"/api/club/{a}/matches").status_code == 401
   ```

   `_match(program_id, *, status=...)` (line 211) and `_headers(key)` (line 174) already exist in
   this file; `club_app.c2["program_a"]` / `["program_b"]` are the two fixture programs.

## How to start

1. `PLAN.md`, at most 10 lines. Then act.
2. Make both test edits. Run `make gate TASK=P0-B1`. RED: the new test gets `405` (no GET route
   yet) and the gated-route test fails on the new tuple entry (405 instead of 403). That is correct.
3. Add the route. Run the gate again. GREEN.

## When things go wrong

- New test still gets `405 METHOD NOT ALLOWED` → the decorator must be `methods=["GET"]` on the
  EXACT path `"/club/<int:program_id>/matches"`; check for a typo.
- `assert [...] == [second.id, first.id]` fails with the order reversed → the `order_by` must be
  `VideoMatch.created_at.desc(), VideoMatch.id.desc()` (ties on created_at break by id).
- The gated-route test gets 200 for your new path → you forgot `@require_club_manager()` or put it
  above the route decorator; the order is route decorator first, then `@require_club_manager()`,
  exactly like the other routes.
- `ruff` complains → only on files you edited; fix and gate again.
- Same error twice → STOP, BLOCKED, paste it.
- After ANY interruption: run the gate; whatever is red is your next step.

## Do not

- Do not add pagination, filters, or roster data. Do not touch other routes, the models, the
  frontend, or `api.js`.

## Done means

1. `make gate TASK=P0-B1` green — you ran it, you saw it.
2. `grep -n 'def list_club_matches' academy-watch-backend/src/routes/club.py` prints one line.
3. Handback file on disk + the `HANDBACK-FILED: .harness/handback/$HARNESS_SESSION.md` last line.
