# Task brief — P0-A11c: a late Introduce send or a late box load never overwrites the current form/box (review follow-up)

**Pattern:** copy-adapt · **Thinking:** off · **Budget:** 20 min ·
**Files you will touch (ALL replaced by shipped files — one command, nothing to type):**
`academy-watch-frontend/src/components/contact/IntroduceDialog.jsx`, `academy-watch-frontend/src/pages/IntroductionsPage.jsx`,
`academy-watch-frontend/tests/introduce-dialog.test.mjs`, `academy-watch-frontend/tests/introductions-page.test.mjs`. Nothing else.

## The situation

Fourth review round (codex, two P2s): (1) closing the Introduce dialog while its POST was in flight and opening it for
another player let the first result overwrite the second form; (2) switching Sent ↔ Inbox before the first load finished
let a late failure from the other box set the error on the current one. The shipped files add an epoch to the dialog
(bumped on open/close/player change) and a load sequence to the page; stale results are discarded. Tests updated.

## Step 0 — replace the four files (one command)

```bash
cp briefs/assets/P0-A11c/IntroduceDialog.jsx academy-watch-frontend/src/components/contact/IntroduceDialog.jsx && cp briefs/assets/P0-A11c/IntroductionsPage.jsx academy-watch-frontend/src/pages/IntroductionsPage.jsx && cp briefs/assets/P0-A11c/introduce-dialog.test.mjs academy-watch-frontend/tests/introduce-dialog.test.mjs && cp briefs/assets/P0-A11c/introductions-page.test.mjs academy-watch-frontend/tests/introductions-page.test.mjs && echo FILES-REPLACED
```

Confirm, read-only: `grep -c "opSeq" academy-watch-frontend/src/components/contact/IntroduceDialog.jsx; grep -c "loadSeq" academy-watch-frontend/src/pages/IntroductionsPage.jsx` → `6` then `5`.

## Step 1 — gate

```bash
make gate TASK=P0-A11c
```

GREEN (node --test ×2, then `pnpm lint` 0 errors + `pnpm build`; 1–4 minutes).

## When things go wrong

- `pnpm lint` error in a replaced file → STOP, say BLOCKED, paste it (the files are shipped; if they lint dirty, the brief is wrong, not you).
- Same error twice → STOP, BLOCKED.

## Do not

- Do not edit any of the four files by hand, or touch anything else.

## Done means

1. `make gate TASK=P0-A11c` green — you ran it, you saw it.
2. The confirm greps print `6` and `5`.
3. Handback on disk at `.harness/handback/$HARNESS_SESSION.md` + the last line `HANDBACK-FILED: .harness/handback/$HARNESS_SESSION.md`.
