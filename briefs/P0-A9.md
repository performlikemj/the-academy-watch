# Task brief — P0-A9: people can find the introductions (nav + Scout Desk header)

**Pattern:** copy-adapt · **Thinking:** off · **Budget:** 45 min ·
**Files you will touch:** `academy-watch-frontend/src/App.jsx` (ONE icon import + ONE nav item line),
`academy-watch-frontend/src/pages/ScoutPage.jsx` (TWO header buttons), and
`academy-watch-frontend/tests/introductions-nav.test.mjs` (NEW). Nothing else.
**Depends on:** P0-A5 (`/scout/verification`) and P0-A6 (`/introductions`) — both landed on this branch.

**Shipped files:** this brief ships its NEW files under `briefs/assets/P0-A9/`; each numbered section below gives the exact `cp` command. Never retype a shipped file — copy it. Your work is the edits inside existing files and the gate.

## The situation

The introductions page and the scout verification page exist, but nothing links to them. Add an
"Introductions" item to the signed-in navigation, and two buttons in the Scout Desk header next to
"Watchlist"/"Lists": "Introductions" and "Get verified".

## The job

### 1. `academy-watch-frontend/src/App.jsx` — icon + nav item (by `sed`; no typing)

Do NOT open `App.jsx` (4,300 lines) and do NOT use your edit tool on it.

**1a. Icon** — the lucide-react import block ends with a line `  Clock` (no comma) followed by
`} from 'lucide-react'`. Check first (read-only):

```bash
LC=$(grep -n "^} from 'lucide-react'" academy-watch-frontend/src/App.jsx | cut -d: -f1); echo "LC=$LC; line before: [$(sed -n "$((LC-1))p" academy-watch-frontend/src/App.jsx)]"
```

If it prints `[  Clock]`, run (one command) — it replaces that line with `  Clock,` + `  Send`:

```bash
LC=$(grep -n "^} from 'lucide-react'" academy-watch-frontend/src/App.jsx | cut -d: -f1); sed -i '' "$((LC-1))d" academy-watch-frontend/src/App.jsx && sed -i '' "$((LC-2))r briefs/assets/P0-A9/lucide-tail.jsx" academy-watch-frontend/src/App.jsx && echo ICON-ADDED
```

If it prints `[  Send]`, the icon is already imported — skip 1a. Anything else → STOP, BLOCKED.

**1b. Nav item** (after the `Lists` push inside `function Navigation()`):

```bash
LP=$(grep -n "items.push({ path: '/scout/lists', label: 'Lists', icon: ListChecks })" academy-watch-frontend/src/App.jsx | cut -d: -f1); echo "LP=$LP"; sed -i '' "${LP}r briefs/assets/P0-A9/nav-item-line.jsx" academy-watch-frontend/src/App.jsx && echo NAV-INSERTED
```

### 2. `academy-watch-frontend/src/pages/ScoutPage.jsx` — two header buttons (by `sed`; no typing)

The desk header has a "Lists" button whose label line is exactly `                Lists` and whose closing
`</Button>` is two lines below it. Insert the shipped two-button block after that `</Button>`:

```bash
LL=$(grep -n '^                Lists$' academy-watch-frontend/src/pages/ScoutPage.jsx | cut -d: -f1); echo "LL=$LL; line LL+2 is: [$(sed -n "$((LL+2))p" academy-watch-frontend/src/pages/ScoutPage.jsx)]"; sed -i '' "$((LL+2))r briefs/assets/P0-A9/header-buttons.jsx" academy-watch-frontend/src/pages/ScoutPage.jsx && echo HEADER-INSERTED
```

The echo must show `line LL+2 is: [            </Button>]`. (`Send` and `ShieldCheck` are ALREADY imported from
lucide-react in ScoutPage.jsx; do not add an import.)

Confirm, read-only:

```bash
grep -c "items.push({ path: '/introductions'" academy-watch-frontend/src/App.jsx; grep -c '<Link to="/introductions"' academy-watch-frontend/src/pages/ScoutPage.jsx; grep -c "Get verified" academy-watch-frontend/src/pages/ScoutPage.jsx
```

Expected: `1`, `1`, `1`. Anything higher means a command ran twice — STOP, BLOCKED.

### 3. Test — create `academy-watch-frontend/tests/introductions-nav.test.mjs` (write it FIRST)

Shipped with this brief — COPY it, never retype (one command):

```bash
mkdir -p academy-watch-frontend/tests && cp briefs/assets/P0-A9/introductions-nav.test.mjs academy-watch-frontend/tests/introductions-nav.test.mjs
```

## How to start

1. `PLAN.md`, at most 5 lines. Then act.
2. Copy the test file (the `cp` command above). Run `make gate TASK=P0-A9`. RED. Correct.
3. Make the App.jsx edits (1a, 1b), then the ScoutPage edit (2). Gate again. GREEN (~70 s).

## When things go wrong

- `pnpm lint`: `'Send' is not defined` in App.jsx → you skipped 1a.
- `pnpm lint`: duplicate identifier `Send` in App.jsx → it was already imported; undo 1a.
- `pnpm build` fails in ScoutPage → the two buttons must sit INSIDE the same flex `<div>` as the Lists
  button, after its closing `</Button>`.
- Same error twice → STOP, BLOCKED, paste it.
- After ANY interruption: run the gate; whatever is red is your next step.

## Do not

- Do not reorder or rename existing nav items. Do not touch the mobile drawer code or routes.

## Done means

1. `make gate TASK=P0-A9` green — you ran it, you saw it.
2. `grep -c "/introductions" academy-watch-frontend/src/App.jsx` prints at least `2` (route + nav item).
3. Handback file on disk + the `HANDBACK-FILED: .harness/handback/$HARNESS_SESSION.md` last line.
