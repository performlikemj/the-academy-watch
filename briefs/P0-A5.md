# Task brief — P0-A5: the scout verification page (`/scout/verification`)

**Pattern:** copy-adapt · **Thinking:** off · **Budget:** 75 min ·
**Files you will touch:** `academy-watch-frontend/src/lib/scout-verification.js` (NEW),
`academy-watch-frontend/src/pages/ScoutVerificationPage.jsx` (NEW), `academy-watch-frontend/src/App.jsx`
(ONE import line + ONE route line), and `academy-watch-frontend/tests/scout-verification-page.test.mjs`
(NEW). Nothing else.
**Depends on:** P0-A2 (`APIService.getScoutVerification` / `submitScoutVerification`) — landed.

**Shipped files:** this brief ships its NEW files under `briefs/assets/P0-A5/`; each numbered section below gives the exact `cp` command. Never retype a shipped file — copy it. Your work is the edits inside existing files and the gate.

## The situation

Only VERIFIED scouts may introduce themselves to players. iOS has a verification form; the web has
none, so a web scout can never get verified. Build the page: it shows the scout's current verification
(none / pending / approved / rejected / revoked) and, when there is no active one, a form that submits
`{ full_name, organization, role_title, statement, evidence_urls }`. The API:

- `GET /api/scout/verification` → `{ "verification": null | { id, full_name, organization, role_title,
  statement, evidence_urls: [...], status: "pending"|"approved"|"rejected"|"revoked", submitted_at,
  reviewed_at, review_notes, revocation_reason } }`
- `POST /api/scout/verification` → 201 `{ "verification": {...} }`; 400 `{ "error": "…" }` on a bad
  field; 409 `{ "error": "an active scout verification already exists", "verification": {...} }`.
  Server limits: full_name ≤ 200, organization ≤ 200, role_title ≤ 120, statement ≤ 2000, 1–10 evidence
  URLs, each an absolute `https://` URL ≤ 500 chars. Rate limit 3/hour.

## Shipped step scripts

Commands that look like `bash briefs/assets/P0-A5/step-N.sh` are the brief's own commands shipped as files.
Run them EXACTLY like that, from the worktree root. Never open, copy out, or retype their contents — they print the
same `X=<n>` / `...-INSERTED` / `...-REPLACED` markers the steps describe. If one prints BLOCKED or an empty `X=`, STOP
and say BLOCKED with the line.

## The job

### 1. Pure helper — create `academy-watch-frontend/src/lib/scout-verification.js`

Shipped with this brief — COPY it, never retype (one command):

```bash
mkdir -p academy-watch-frontend/src/lib && cp briefs/assets/P0-A5/scout-verification.js academy-watch-frontend/src/lib/scout-verification.js
```

### 2. Page — create `academy-watch-frontend/src/pages/ScoutVerificationPage.jsx`

Copy-adapt of `src/pages/ClaimAccount.jsx` (Card layout) plus the `Input`/`Textarea` controls other pages
use. Copy exactly:

Shipped with this brief — COPY it, never retype (one command):

```bash
mkdir -p academy-watch-frontend/src/pages && cp briefs/assets/P0-A5/ScoutVerificationPage.jsx academy-watch-frontend/src/pages/ScoutVerificationPage.jsx
```

### 3. Route — `academy-watch-frontend/src/App.jsx` (two one-line insertions, by `sed`; no typing)

Do NOT open `App.jsx` (4,300 lines) and do NOT use your edit tool on it. Run these two commands exactly;
each finds its anchor line with `grep -n` and inserts the shipped one-line snippet after it:

```bash
bash briefs/assets/P0-A5/step-1.sh
```

```bash
bash briefs/assets/P0-A5/step-2.sh
```

Confirm, read-only: `grep -n "ScoutVerificationPage" academy-watch-frontend/src/App.jsx` → exactly two lines
(import near the top, route near the bottom). Four lines means a command ran twice — STOP, BLOCKED.

### 4. Test — create `academy-watch-frontend/tests/scout-verification-page.test.mjs` (write it FIRST)

Shipped with this brief — COPY it, never retype (one command):

```bash
mkdir -p academy-watch-frontend/tests && cp briefs/assets/P0-A5/scout-verification-page.test.mjs academy-watch-frontend/tests/scout-verification-page.test.mjs
```

## How to start

1. `PLAN.md`, at most 10 lines. Then act.
2. Copy the test file (the `cp` command above). Run `make gate TASK=P0-A5`. RED: `Cannot find module '../src/lib/scout-verification.js'`.
3. Copy the helper, then the page (their `cp` commands), then the two App.jsx lines. Gate again. GREEN (~70 s with lint+build).

## When things go wrong

- `pnpm lint` unused-var → every import in the page is used above; restore JSX rather than deleting imports.
- `assert.deepEqual(ok.payload, …)` fails → `buildVerificationPayload` must TRIM every field and keep the
  evidence order of first appearance; copy the helper byte-for-byte.
- `pnpm build` fails in App.jsx → the two added lines sit exactly where stated; nothing else changes.
- Same error twice → STOP, BLOCKED, paste it.
- After ANY interruption: run the gate; whatever is red is your next step.

## Do not

- Do not touch nav, ScoutPage, the backend, or other pages. Do not add file uploads.

## Done means

1. `make gate TASK=P0-A5` green — you ran it, you saw it.
2. `grep -n "scout/verification" academy-watch-frontend/src/App.jsx` prints one route line.
3. Handback file on disk + the `HANDBACK-FILED: .harness/handback/$HARNESS_SESSION.md` last line.
