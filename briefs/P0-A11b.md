# Task brief — P0-A11b: a send that finishes after switching threads never lands in the new thread (review follow-up)

**Pattern:** copy-adapt · **Thinking:** off · **Budget:** 20 min ·
**Files you will touch:** `academy-watch-frontend/src/components/contact/ContactThread.jsx` and
`academy-watch-frontend/tests/contact-thread.test.mjs` — BOTH replaced by shipped files (two `cp`s, nothing to type). Nothing else.

## The situation

Third review round (codex, P1): the GET sequencing from P0-A11 covered loads, but a message POST for thread A still
in flight while the user switched to thread B would append A's message to B and clear B's draft. The shipped
component remembers which thread a send / outcome report was for (`requestIdRef`), discards stale results, and resets
the in-flight flags when the thread changes. Test updated.

## Step 0 — replace the two files (one command)

```bash
cp briefs/assets/P0-A11b/ContactThread.jsx academy-watch-frontend/src/components/contact/ContactThread.jsx && cp briefs/assets/P0-A11b/contact-thread.test.mjs academy-watch-frontend/tests/contact-thread.test.mjs && echo FILES-REPLACED
```

Confirm, read-only: `grep -c "requestIdRef" academy-watch-frontend/src/components/contact/ContactThread.jsx` → `7`.

## Step 1 — gate

```bash
make gate TASK=P0-A11b
```

GREEN (node --test, then `pnpm lint` 0 errors + `pnpm build`; 1–4 minutes).

## When things go wrong

- `pnpm lint` error in the component → STOP, say BLOCKED, paste it (the file is shipped; if it lints dirty, the brief is wrong, not you).
- Same error twice → STOP, BLOCKED.

## Do not

- Do not edit either file by hand, or touch anything else.

## Done means

1. `make gate TASK=P0-A11b` green — you ran it, you saw it.
2. The confirm grep prints `7`.
3. Handback on disk at `.harness/handback/$HARNESS_SESSION.md` + the last line `HANDBACK-FILED: .harness/handback/$HARNESS_SESSION.md`.
