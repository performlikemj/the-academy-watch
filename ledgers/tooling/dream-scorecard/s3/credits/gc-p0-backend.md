# GC-P0 — GOL credits backend

Worktree: __WT__ (branch `feat/gol-credits-backend`, base origin/main). Read first: `CLAUDE.md`, `docs/agents/backend.md`, `docs/agents/invariants.md`, `ledgers/DIRECTIVE_gol-credits.md`, `ledgers/tooling/dream-scorecard/s3/s3-brief-common.md` (fences/gates/port fence still apply; head is `s3c1`), `ledgers/tooling/dream-scorecard/s3/s3-contracts.md` (the billing foundation you extend), and THIS package's contract `ledgers/tooling/dream-scorecard/s3/credits/gc-contracts.md` (authoritative for every name, shape, signature and ordering rule — follow it literally).

## Requirements (each done or reported as not done)
1. Catalog per contract, including the `offered_products()/resolve_price()/product_for_price_id()` guard for entries without `prices`, `purchase_mode` defaulting to subscription, `offered_packs()`, USD-only packs, `/api/billing/config.packs`.
2. `GolCreditLedger` (new file `src/models/gol_credits.py`, registered like the billing models) + migration `s3d1` (down `s3c1`; guarded like `s3b1`; RLS on; `flask db heads` → exactly `s3d1`).
3. `src/services/gol_credits.py` exactly per contract (commit ownership: reserve/refund commit; grant/apply_refund flush only). Dialect-neutral; savepoint + IntegrityError loser path on the debit insert.
4. `create_checkout` with the contract's signature and XOR validation; payment branch rules (exact-match reuse, `client_key_conflict`, no sibling expiry, no already_subscribed, metadata on Session AND PaymentIntent). Route forwards `pack_id`.
5. Webhook handling per contract (`checkout.session.completed` payment mode requiring the local row; `async_payment_succeeded`; `charge.refunded`), inside the existing single-commit / failed-retry / user-lock-first plumbing.
6. `POST /api/gol/chat` per contract — parse → validate → GolService init → reserve → 402 or stream; `usage` first frame; compensation BEFORE yielding the terminal error (wrap the service generator; also the route's own exception path); never on disconnect; `require_scout_entitlement("gol_chat")` removed from chat AND export-pdf; NO `require_billing_rail` on either.
7. Entitlements + `/api/billing/me.gol` + admin summary `gol` per contract; billing-off path makes no ledger query.
8. Account export/delete per contract (explicit ledger delete + count before the FK scan; payment-mode preflight; forfeiture event after redaction with `user_email=None`).
9. Tests `tests/test_gol_credits.py` (new; module-local app registering `gol_bp, billing_bp, account_bp, scout_bp, auth_bp`; stub `GolService` so no network — read how tests/test_gol_access.py stubs it): dark → chat works without `client_msg_id`, no ledger rows, no ledger query (assert via a query counter or a monkeypatched `balances`); lit → 3 free then 402 with NO SSE bytes; same `client_msg_id` twice → one debit (`debited:false` second time); terminal error → one reversal keyed `refund:<debit_id>`, a second error frame does not double-refund, and a RETRY after the refund debits again as attempt 2; admin → never debits; export-pdf → never debits; starter checkout (mocked Stripe) → mode payment, metadata on both objects, no already_subscribed; same client_key different pack → 409; paid completed → +credits from env, replay → no second grant, missing local row → event failed; unpaid completed → no grant; async_payment_succeeded → one grant; refunds: 7-credit grant refunded 50% then 100% → reversals 3 then 4 (target math), replay → 0, `refunded_cents` tracks cumulative; negative balance in admin summary; `/api/billing/me.gol` and `/auth/me` numbers agree; account delete forfeits and counts; SQLite caveat documented + IntegrityError loser-path test. Update `tests/test_gol_access.py` / `tests/test_billing.py` / `tests/test_scout_entitlements.py` where the contract changes behaviour (features map, lit chat payloads now need `client_msg_id`, old 403 cases become free-allowance successes; fixture env cleanup for the new GOL envs) — narrowly.

## ALLOWED
`academy-watch-backend/src/config/stripe_config.py`, `src/models/gol_credits.py` (new), `src/services/gol_credits.py` (new), `src/services/stripe_billing.py`, `src/services/scout_entitlements.py`, `src/routes/billing.py`, `src/routes/gol.py`, `src/routes/scout.py` (entitlements payload only), `src/routes/auth_routes.py` (features only), `src/services/account.py`, `src/main.py` (model registration only, if that is where billing models are registered), `migrations/versions/s3d1_gol_credit_ledger.py` (new), `tests/test_gol_credits.py` (new), and narrowly-scoped edits (assertions, request payloads, fixture env cleanup) in `tests/test_gol_access.py`, `tests/test_billing.py`, `tests/test_scout_entitlements.py`. Nothing else. No frontend.

## Gates
As s3-brief-common (ruff + format; the four named test files + `tests/test_contact.py`; `flask db heads` → `s3d1`). Final report ≤30 lines: per requirement, commit hash, gate counts, the exact SQLite caveat wording you used, anything not done.

## Commit (exactly one)
`feat(billing): GOL credits — 3 free questions, $20 starter + top-up packs via payment-mode Checkout, idempotent debits/refunds, dark behind BILLING_ENABLED`
