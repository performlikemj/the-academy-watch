# GC-P1 — GOL credits web

Worktree: __WT__ (branch `feat/gol-credits-web`, base origin/main). Read first: `CLAUDE.md`, `docs/agents/frontend.md`, `ledgers/DIRECTIVE_gol-credits.md`, `ledgers/tooling/dream-scorecard/s3/s3-brief-common.md` (PORT FENCE: your Vite port is **5198**; never 5001/5173), and the contract `ledgers/tooling/dream-scorecard/s3/credits/gc-contracts.md` — mock every response byte-for-byte. The backend (GC-P0) is built in parallel; never call it. `GET /api/billing/config` 404 = dark → today's UI everywhere. **Credit UI renders ONLY when `auth.scoutPro?.enabled === true` and the user is not an admin** (numeric fields exist in dark mode too, so never infer "lit" from their presence).

## Requirements
1. `src/lib/api.js`: `getBillingConfig` / `getBillingMe` / `createBillingCheckout` already pass JSON through — no change beyond a comment. `streamChat`: accept and send `client_msg_id` on every question; on HTTP **402** emit the typed access event (like 401/403) carrying the parsed body; leave SSE parsing to the hook.
2. `src/hooks/useGolChat.js`: mint a `client_msg_id` (`crypto.randomUUID().replace(/-/g,'')`) per submitted question; keep a **failed-attempt record** (content, pre-attempt history, client_msg_id) and expose `retryFailedMessage()` + a Retry control that resends that exact snapshot with the SAME id (retyping is a new question with a new id); parse `event: usage` frames into `freeQuestionsRemaining` / `creditBalance` state (also the `refunded: true` frame); on 402 remove the pending bubbles and surface the balances from the body.
3. `src/components/gol/*`: when lit (rule above) fetch `/api/billing/config` once for pack terms; status line "3 free questions left" → "Credits: 97"; exhausted state = composer disabled + copy built from the config ("Buy the Starter — N questions for $20"; generic copy when amounts are absent) + a link to the 402 body's `top_up_path` (always honour it; no "never bought" branch). **PDF export stays ENABLED in the exhausted state** — remove the Scout Pro/PDF lock copy in `GolChatWindow`. Admins and dark: nothing changes. Never render a raw error code.
4. `src/pages/PricingPage.jsx`: when `config.packs` is non-empty AND `config.products` is empty, the Scout Pro card becomes a **GOL Credits** card: starter pack (formatted amount + "N questions") with a Buy CTA → `createBillingCheckout({ pack_id, client_key })` (sessionStorage-reused key per pack, cleared on success like today) → `window.location.assign`; pick `gol_starter` when `/api/billing/me.gol.purchases` is empty (fetch it when signed in), else `gol_topup` labelled "Buy more". Dark → the static beta card, unchanged.
5. `src/pages/AccountBillingPage.jsx`: hide the legacy Scout Pro / subscription sections when `config.products` is empty (keep them when products or subscription rows exist); add a GOL section — free questions remaining, credit balance, Buy/Top-up button, purchase list (date, pack, credits, amount+currency, refunded credits). On `?checkout=success&session_id=…`: poll `/api/billing/me` (up to 6 tries over ~12 s) until a purchase with that `stripe_session_id` appears (handle "already present on first fetch"), show "Added N credits", then call `APIService.getProfile()` so the auth snapshot refreshes and the panel's exhausted gate clears; keep the existing `checkout_completed` analytics and key clearing; strip the query only after the match (or after the last poll).
6. `e2e/billing.spec.mjs` (update the run-header port to 5198): dark unchanged; lit → status line 3 free; three questions decrement via `usage`; fourth → 402 → exhausted state, no bubble, link to top_up_path, PDF still enabled; Retry reuses the same `client_msg_id` (assert the POST bodies), a new typed question uses a new id; pricing shows the GOL Credits card and posts `{pack_id, client_key}`; account billing shows balance/purchases and the success poll → "Added N credits" and a `/auth/me` refetch.

## ALLOWED
`academy-watch-frontend/src/lib/api.js`, `src/hooks/useGolChat.js`, `src/components/gol/*`, `src/pages/PricingPage.jsx`, `src/pages/AccountBillingPage.jsx`, `src/context/AuthContext.jsx`, `src/context/buildAuthSnapshot.js`, `e2e/billing.spec.mjs`. No package.json/lockfile. Gates: `./scripts/setup_frontend.sh`; `pnpm lint` (0 errors) && `pnpm build`; the spec on your own Vite 5198, stopped by PID. Final report ≤25 lines.

## Commit (exactly one)
`feat(web): GOL credits — free-question counter, 402 out-of-credits state, retry with the same question id, starter/top-up packs on pricing and account billing`
