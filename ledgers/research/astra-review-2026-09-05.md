# Independent review — gpt-6-astra (codex, read-only) — 2026-09-05

Brief: `ledgers/research/astra-review-brief-2026-09-05.md`. Checkout reviewed: origin/main fe7d25f. Verdict, criterion audit, defects, plan to 100.

---

## Verdict

- **My evidence-adjusted score is 65.1%, versus the published 65.6%:** 3.6 and 5.5 fall from 3→2 because billing is dark; 2.7 rises from 1→2 because coaching briefs now exist. Calculated using the repository’s scoring formula. (`ledgers/tooling/dream-scorecard/score.py:54`)
- This is a **code audit with historical deployment evidence**, not independent certification of current production. In particular, the last recorded public iOS build was broken; its replacement was submitted for review. (`CONTINUITY.md:100`)
- **First:** fix GOL replay, refund ordering, purchase snapshots, and streaming before enabling payments. (`academy-watch-backend/src/routes/gol.py:74`; `academy-watch-backend/src/services/stripe_billing.py:689`)
- **Second:** release and verify the repaired iOS app, then finish the player-owned profile/game journeys. (`CONTINUITY.md:100`; `academy-watch-ios/AcademyWatch/Features/PlayerDetail/PlayerDetailView.swift:290`)
- **Third:** deliver coaching feedback to players through the existing club console. (`academy-watch-backend/src/routes/club.py:879`)
- **Fourth:** make club-entered statistics trustworthy and correctable before expanding club analytics. (`academy-watch-backend/src/routes/club.py:821`; `academy-watch-backend/src/routes/club.py:936`)
- **Fifth:** recruit one real cohort while shipping these changes; another engineering stage cannot earn adoption. (`ledgers/GRADING_dream-scorecard-2026-09-02.md:97`)
- **Ceiling:** 94.2% under the conservative acceptance assumptions below; the rubric’s purely mathematical ceiling with only adoption unearned is 98.6%. Neither is 100%, which requires actual participation, payments, club support, and lawful ownership—not just their interfaces.

## Part 1 — Criterion audit

The actual rubric is **0–4**, not 0–3, and the compiled scorecard contains **39 criteria**, not the 31/32 mentioned in its prose. A 2 includes flag-off and admin-per-use functionality; a 3 requires a usable production journey on at least one client; a 4 requires all relevant clients, tests, deployment, and no known safety or correctness caveat. (`ledgers/GRADING_dream-scorecard-2026-09-02.md:15`; `ledgers/GRADING_dream-scorecard-2026-09-02.md:123`; `ledgers/tooling/dream-scorecard/score.py:56`)

For compactness, reach labels below mean:

- **W:** `LIVE_WEB`.
- **WI:** `LIVE_WEB_IOS`.
- **A:** `ADMIN_ONLY`.
- **D:** `DESIGNED_ONLY`.
- **M:** `MISSING`.
- **Dark:** implemented but flag-off; a descriptive audit status, not a new scoring tier.
- **W†:** web reach retained from the repository’s deployment evidence; native implementation exists, but public iOS availability is **UNCONFIRMED**.

The last dated release record says iOS 1.0.1/build 8 was submitted to App Review. The preceding record says public 1.0/build 7 had both a retired API host and a season-statistics decoding failure. Therefore, I cannot certify any present-tense `LIVE_WEB_IOS` claim from this checkout alone. I retain grade 3 where the web independently completes the journey. (`CONTINUITY.md:100`; `CONTINUITY.md:102`)

I distinguish one-time identity verification from an administrator having to operate the product on every use. That preserves the existing grade-3 interpretation of claims and club access, while retaining grade 2 for Film Room processing.

The scoring tool also explains much of the contradictory evidence: overrides update scores and selected fields while retaining older `exists`, `missing`, and `next_step` text. Thus “no local discovery” and “no iOS fan surface” survive alongside code implementing them. (`ledgers/tooling/dream-scorecard/score.py:24`; `ledgers/tooling/dream-scorecard/score.py:41`)

### P1 — Players: share, be found, have fans

**Reviewed pillar score: 75.0%.**

| ID / criterion | Claimed grade/reach | My grade/reach | Verdict | Evidence and remaining limitation |
|---|---:|---:|---|---|
| **1.1 Create or claim my profile** | 3 / WI | 3 / W† | **OVERSTATED reach; grade confirmed** | Local creation and adult self-claim checks exist. Native onboarding now supplies birth date, contradicting the recorded omission. Initial review remains. Public native release is unconfirmed. `academy-watch-backend/src/routes/showcase.py:1514`; `academy-watch-backend/src/routes/showcase.py:1561`; `academy-watch-ios/AcademyWatch/Features/Onboarding/OnboardingViewModels.swift:117`; `CONTINUITY.md:100`. |
| **1.2 Share about myself** | 3 / W | 3 / W | **CONFIRMED; blocker text stale** | Low-risk edits can remain approved after eligibility checks; sensitive changes still return the profile to pending. The ledger records deployment of the trust setting, so “must still set the flag” is stale. Native claim attestation is not a complete bio/photo/reel editor. `academy-watch-backend/src/routes/showcase.py:310`; `academy-watch-backend/src/routes/showcase.py:2795`; `ledgers/CONTINUITY_dream-s1.md:74`; `academy-watch-ios/AcademyWatch/Features/Showcase/PlayerClaimViewModel.swift:79`. |
| **1.3 Add my own games and stats** | 3 / W | 3 / W | **UNDERSTATED implementation; grade unchanged** | Owner CRUD and reported-stat rollups exist. iOS now has an Add Game path, but only for owned negative IDs; owned provider-backed players are excluded and native editing/deletion is incomplete. `academy-watch-backend/src/routes/player_matches.py:231`; `academy-watch-backend/src/routes/player_matches.py:339`; `academy-watch-backend/src/services/season_rollup_service.py:758`; `academy-watch-ios/AcademyWatch/Features/PlayerDetail/PlayerDetailView.swift:290`. |
| **1.4 Be found in scout discovery** | 3 / WI | 3 / W† | **OVERSTATED live reach; implementation confirmed** | Approved public adult local subjects join the scout universe with signed IDs and provenance. Native model support exists, but the source/category presentation is thinner and the released app is unconfirmed. `academy-watch-backend/src/routes/scout.py:431`; `academy-watch-backend/src/routes/scout.py:818`; `academy-watch-ios/AcademyWatch/Core/Models/ScoutModels.swift:83`; `CONTINUITY.md:100`. |
| **1.5 Be contacted safely** | 3 / WI | 3 / W† | **OVERSTATED live reach; stale local-player limitation** | The rail now resolves local subjects and revalidates claims under lock. Native accept/decline exists. This earns 3, not comprehensive safety: scout credential/adult verification and some subject-eligibility gaps remain. `academy-watch-backend/src/routes/contact.py:420`; `academy-watch-backend/src/routes/contact.py:441`; `academy-watch-backend/src/routes/contact.py:462`; `academy-watch-ios/AcademyWatch/Features/Contact/IncomingContactRequestsView.swift:249`. |
| **1.6 Know I am being watched** | 3 / WI | 3 / W† | **OVERSTATED parity/reach** | The web owner card exposes richer aggregates; the native interest card retains older counts. Anonymous profile views are not proof of verified-scout attention. `academy-watch-backend/src/routes/showcase.py:1996`; `academy-watch-frontend/src/components/ShowcaseSection.jsx:1626`; `academy-watch-ios/AcademyWatch/Features/Showcase/PlayerInterestSignalsCard.swift:124`; `ledgers/GRADING_dream-scorecard-2026-09-02.md:44`. |
| **1.7 Have fans** | 3 / W | 3 / W | **UNDERSTATED implementation; grade unchanged** | The fan count and follow/unfollow API exist, and native fan controls now call them. “iOS has no fan surface” is false for this source revision. This does not establish real fans or public native release. `academy-watch-backend/src/routes/players.py:55`; `academy-watch-backend/src/routes/players.py:74`; `academy-watch-ios/AcademyWatch/Features/PlayerDetail/PlayerFanSection.swift:45`; `academy-watch-ios/AcademyWatch/Features/PlayerDetail/PlayerFanSection.swift:95`. |
| **1.8 Player growth view** | 3 / WI | 3 / W† | **OVERSTATED live reach; understated local support** | Reported local/club cells now feed season totals; growth is no longer exclusively provider-fed. Native season/journey views exist, but provenance labels do not fully explain the new sources, and the post-entry refresh has a race described below. `academy-watch-backend/src/services/season_rollup_service.py:680`; `academy-watch-backend/src/services/season_rollup_service.py:782`; `academy-watch-ios/AcademyWatch/Features/PlayerDetail/PlayerDetailView.swift:257`; `academy-watch-ios/AcademyWatch/Core/Models/SeasonModels.swift:36`. |

### P2 — Clubs: track, analyze growth, tools to improve

**Reviewed pillar score: 66.7%, versus 63.9%.**

| ID / criterion | Claimed grade/reach | My grade/reach | Verdict | Evidence and remaining limitation |
|---|---:|---:|---|---|
| **2.1 Claim, verify my club, and reach the console** | 3 / W | 3 / W | **CONFIRMED** | Club-official approval now grants console access. Manager authorization checks the active grant and approved underlying program/claim. Initial verification remains; this is not unrestricted self-service club creation. `academy-watch-backend/src/routes/showcase.py:3337`; `academy-watch-backend/src/routes/showcase.py:3378`; `academy-watch-backend/src/services/club_registry.py:138`. |
| **2.2 Roster, adoption, tracked links, squads** | 3 / W | 3 / W | **CONFIRMED, partial scope** | Managers can maintain a roster, but local adoption remains creator-only. A player-created profile cannot simply accept a club invitation because that invitation/acceptance journey is absent. Provider-player attachment also has an attribution weakness described below. `academy-watch-backend/src/routes/club.py:821`; `academy-watch-backend/src/routes/club.py:834`; `academy-watch-backend/src/routes/club.py:842`. |
| **2.3 Track games without video** | 3 / W | 3 / W | **CONFIRMED, with correctness defects** | Result entry and club-confirmed rollup contributions are real. The optional video association is explicitly not persisted. Corrections cannot reliably remove an appearance or change the fixture identity. `academy-watch-backend/src/routes/club.py:936`; `academy-watch-backend/src/routes/club.py:941`; `academy-watch-backend/src/routes/club.py:1041`; `academy-watch-backend/src/routes/club.py:1160`. |
| **2.4 Track academy graduates and loanees** | 3 / W | 3 / W | **CONFIRMED for covered clubs, not a complete console feature** | Public provider-backed team/pathway pages support the job. MyClub still lacks the corresponding manager-scoped pathway view; community rosters do not inherit one merely by existing. `academy-watch-backend/src/routes/api.py:12853`; `academy-watch-frontend/src/pages/TeamDetailPage.jsx:438`; `academy-watch-frontend/src/pages/MyClubConsole.jsx:2099`. |
| **2.5 Analyze growth and compare roster players** | 3 / WI | 3 / W† | **OVERSTATED reach; grade confirmed** | Season rollups and native comparison are substantial. Local comparison support is newer than the evidence text suggests. There is still no cohesive manager-roster growth workspace. `academy-watch-backend/src/services/season_rollup_service.py:782`; `academy-watch-backend/src/routes/scout.py:1354`; `academy-watch-ios/AcademyWatch/Features/Compare/CompareView.swift:201`; `academy-watch-frontend/src/pages/MyClubConsole.jsx:2099`. |
| **2.6 Film Room upload, analysis, reports, reels** | 2 / A | 2 / A | **CONFIRMED** | Club processing requests still only record a timestamp and return acceptance; they do not enqueue analysis. Upload/consumption screens do not make the complete workflow self-service. `academy-watch-backend/src/routes/club.py:1455`; `academy-watch-backend/src/routes/club.py:1174`; `academy-watch-frontend/src/components/video/PlayerReel.jsx:796`. |
| **2.7 Tools to improve players** | 1 / W | **2 / W authoring; A-dependent analysis** | **UNDERSTATED grade** | Managers now persist per-player coaching briefs and a club system brief; the UI displays analysis checks against them. This is more than a stub. It is not yet a permissioned development record delivered to the player, and analysis remains dependent on the Film Room workflow. `academy-watch-backend/src/routes/club.py:879`; `academy-watch-backend/src/routes/club.py:898`; `academy-watch-frontend/src/pages/MyClubConsole.jsx:549`; `academy-watch-frontend/src/components/video/PlayerReel.jsx:158`. |
| **2.8 Tools to improve the club** | 2 / W | 2 / public W; manager workflow incomplete | **CONFIRMED grade; reach needs qualification** | Public benchmarks and the system brief do not constitute a manager-owned club improvement dashboard. The console has operational tabs but no club analytics workspace. `academy-watch-backend/src/routes/club.py:898`; `academy-watch-frontend/src/pages/MyClubConsole.jsx:2099`; `ledgers/GRADING_dream-scorecard-2026-09-02.md:59`. |
| **2.9 Club-to-scout consent loop** | 3 / W | 3 / W | **CONFIRMED** | The web club inbox lists requests and posts consent decisions. Native request boxes expose sent/inbox, not the club box. `academy-watch-frontend/src/components/contact/ClubIntroductionsPanel.jsx:36`; `academy-watch-frontend/src/components/contact/ClubIntroductionsPanel.jsx:53`; `academy-watch-backend/src/routes/contact.py:934`; `academy-watch-ios/AcademyWatch/Core/Models/FullCircleModels.swift:84`. |

### P3 — Scouts: find and analyze

**Reviewed pillar score: 71.4%, versus 75.0%.**

| ID / criterion | Claimed grade/reach | My grade/reach | Verdict | Evidence and remaining limitation |
|---|---:|---:|---|---|
| **3.1 Scout account and verification** | 3 / WI | 3 / W† | **OVERSTATED live reach; grade confirmed** | Verification is derived from revocable approval, not payment. Review does not establish the structured adult/authoritative-credential evidence needed for full marks. `academy-watch-backend/src/services/trust.py:14`; `academy-watch-backend/src/routes/trust.py:200`; `academy-watch-backend/src/routes/trust.py:301`; `CONTINUITY.md:100`. |
| **3.2 Find players** | 3 / WI | 3 / W† | **OVERSTATED live reach; stale evidence** | “User-created players remain invisible” is no longer true: eligible locals enter discovery and comparison. Remaining gaps concern completeness and native presentation, not rebuilding discovery. `academy-watch-backend/src/routes/scout.py:431`; `academy-watch-backend/src/routes/scout.py:709`; `academy-watch-backend/src/routes/scout.py:1354`; `academy-watch-ios/AcademyWatch/Core/Models/ScoutModels.swift:83`. |
| **3.3 Analyze players** | 3 / WI | 3 / W† | **OVERSTATED live reach; grade confirmed** | Existing statistical comparison is useful. Public verified-footage projection excludes private club footage and does not expose the full grounded analysis as a scout-facing report. `academy-watch-ios/AcademyWatch/Features/Compare/CompareView.swift:201`; `academy-watch-backend/src/routes/showcase.py:1306`; `academy-watch-backend/src/routes/showcase.py:1330`. |
| **3.4 Organize prospects and receive alerts** | 3 / WI | 3 / W† | **OVERSTATED live reach; grade confirmed** | Watchlist/list journeys exist and native smoke tests exercise reversible mutations. Scheduled digest deployment exists. This does not prove real subscriptions, successful delivery, or push support. `academy-watch-backend/src/routes/scout.py:1594`; `academy-watch-ios/AcademyWatchUITests/SmokeUITests.swift:174`; `academy-watch-ios/AcademyWatchUITests/SmokeUITests.swift:216`; `.github/workflows/deploy.yml:225`. |
| **3.5 Reach out** | 3 / WI | 3 / W† | **OVERSTATED live reach; understated local support** | Verified scouts can initiate requests to eligible signed subjects, and accepted conversations have participant-gated messaging. The “tracked players only” blocker is stale. `academy-watch-backend/src/routes/contact.py:420`; `academy-watch-backend/src/routes/contact.py:441`; `academy-watch-backend/src/routes/contact.py:1090`; `academy-watch-backend/src/routes/contact.py:1125`. |
| **3.6 Pay for Scout Pro** | **3 / D** | **2 / Dark** | **OVERSTATED grade; understated implementation label** | Checkout, credits and entitlement code exist, but the billing guard hides the rail when disabled, and the ledger explicitly records dark production. The owner re-scoped the paid offering to GOL: do not count legacy subscription scaffolding as a launched Scout Pro product. `academy-watch-backend/src/routes/billing.py:38`; `academy-watch-backend/src/services/scout_entitlements.py:107`; `CONTINUITY.md:104`; `ledgers/GRADING_dream-scorecard-2026-09-02.md:71`. |
| **3.7 Club managers recruit as scouts** | 3 / WI | 3 / W† | **OVERSTATED live reach; grade confirmed** | Manager/scout personas and the verified scout rail can coexist. Requests still do not bind outreach to an explicitly selected, authorized managed club. Payment does not grant verification. `academy-watch-backend/src/services/account_roles.py:18`; `academy-watch-backend/src/services/trust.py:14`; `academy-watch-backend/src/routes/contact.py:478`; `ledgers/GRADING_dream-scorecard-2026-09-02.md:72`. |

### P4 — Funding: support through to club part-ownership

**Reviewed pillar score: 46.4%.**

| ID / criterion | Claimed grade/reach | My grade/reach | Verdict | Evidence and remaining limitation |
|---|---:|---:|---|---|
| **4.1 Club funding registry/programs** | 3 / W | 3 / W | **CONFIRMED** | Managers edit pending profile revisions; approved revisions feed public program pages. The registry is not a working native funding product or a discoverable, populated marketplace. `academy-watch-backend/src/routes/club.py:701`; `academy-watch-backend/src/routes/club.py:721`; `academy-watch-backend/src/routes/funding.py:1134`; `academy-watch-backend/src/routes/funding.py:1254`. |
| **4.2 Money movement** | 1 / D | 1 / D | **CONFIRMED** | Club funding remains non-fundable. GOL purchases buy platform credits; they are not supporter donations, club transfers, or a donation refund/receipt lifecycle. `academy-watch-backend/src/models/funding.py:272`; `academy-watch-backend/src/services/stripe_billing.py:689`; `academy-watch-frontend/src/pages/ProgramPage.jsx:164`. |
| **4.3 Recurring support** | 0 / M | 0 / M | **CONFIRMED** | The billing catalog has platform products, not a ClubProgram supporter subscription lifecycle. Program updates exist, but updates alone do not implement recurring support. `academy-watch-backend/src/config/stripe_config.py:17`; `academy-watch-backend/src/config/stripe_config.py:26`; `academy-watch-backend/src/routes/club.py:765`; `ledgers/GRADING_dream-scorecard-2026-09-02.md:80`. |
| **4.4 External platform connection** | 3 / **M** | 3 / **W** | **UNDERSTATED reach** | Strictly validated external-support links are moderated and exposed from approved revisions; ProgramPage renders them. “MISSING” contradicts the implementation. This is a link-out, not supporter synchronization. `academy-watch-backend/src/routes/club.py:189`; `academy-watch-backend/src/routes/funding.py:1270`; `academy-watch-frontend/src/pages/ProgramPage.jsx:164`. |
| **4.5 Part-ownership/investment** | 0 / M | 0 / M | **CONFIRMED** | No shipped ownership mechanism is evidenced. The rubric itself requires jurisdiction-specific scoping before the mechanism. Club subscriptions and membership benefits cannot silently substitute for ownership. `ledgers/GRADING_dream-scorecard-2026-09-02.md:82`; `academy-watch-backend/src/models/funding.py:248`; `academy-watch-backend/src/models/funding.py:272`. |
| **4.6 Supporter/fan-facing club page** | 3 / W | 3 / W | **CONFIRMED** | Approved updates and external support links render on the public page. Internal funding remains unavailable; discovery and real supporter use remain unproven. `academy-watch-backend/src/routes/funding.py:1278`; `academy-watch-frontend/src/pages/ProgramPage.jsx:147`; `academy-watch-frontend/src/pages/ProgramPage.jsx:164`. |
| **4.7 Grassroots attested manual-data path** | 3 / W | 3 / W | **CONFIRMED, with material caveats** | Club-entered appearances feed the shared rollup with source separation. Entry-time attestation, reliable corrections and trustworthy club attribution remain incomplete. Native source badges also do not substantiate “clear provenance on every surface.” `academy-watch-backend/src/routes/club.py:1062`; `academy-watch-backend/src/services/season_rollup_service.py:770`; `academy-watch-ios/AcademyWatch/Core/Models/SeasonModels.swift:36`. |

### P5 — Foundation

**Reviewed pillar score: 56.2%, versus 59.4%.**

| ID / criterion | Claimed grade/reach | My grade/reach | Verdict | Evidence and remaining limitation |
|---|---:|---:|---|---|
| **5.1 Minor safety** | 2 / WI | 2 / partial W; iOS release unconfirmed | **CONFIRMED grade; evidence stale** | Adult local self-claims now have a gate, and web account controls exist. That does not close non-self claim eligibility, video rights/age/consent, or the future U18 participation model. `academy-watch-backend/src/routes/showcase.py:1561`; `academy-watch-backend/src/routes/showcase.py:1566`; `academy-watch-backend/src/routes/club.py:1253`; `academy-watch-frontend/src/components/AccountDataControls.jsx:41`. |
| **5.2 Data correctness and dispute** | 3 / W | 3 / W | **CONFIRMED, with disconnected resolution** | Users can submit flags, but resolving a flag changes its status/admin note rather than applying and auditing the underlying correction. New manual statistics introduce additional correction gaps. `academy-watch-backend/src/routes/api.py:6590`; `academy-watch-backend/src/routes/api.py:6714`; `academy-watch-backend/src/routes/club.py:1041`. |
| **5.3 Reach** | 3 / W | 3 / W | **CONFIRMED, incomplete coverage** | Share HTML, images, sitemap and robots routes exist. The sitemap has a fixed candidate cap; activity-email pagination can starve later users. A route’s existence is not complete indexing or delivery. `academy-watch-backend/src/routes/share.py:28`; `academy-watch-backend/src/routes/share.py:48`; `academy-watch-backend/src/services/sitemap_service.py:90`; `academy-watch-backend/src/jobs/run_profile_activity_notifications.py:76`. |
| **5.4 Ops and scale** | 2 / A | 2 / A | **CONFIRMED grade; one stated defect fixed** | Queue fencing and a worker exist, but club processing still requires an administrator and managed worker operation is unverified. The claim that deployment omits the maintenance job is stale: it is now included in the image-update loop. `academy-watch-backend/src/services/video_queue.py:83`; `academy-watch-backend/src/workers/vision_worker.py:522`; `academy-watch-backend/src/routes/club.py:1455`; `.github/workflows/deploy.yml:225`. |
| **5.5 Revenue rails** | **3 / A** | **2 / Dark** | **OVERSTATED grade and live availability** | Signed webhooks, persistence and credit logic are built. The same dark guard hides customer and admin billing endpoints. No live financial acceptance is established, and the new GOL lifecycle has defects below. `academy-watch-backend/src/routes/billing.py:38`; `academy-watch-backend/src/services/stripe_billing.py:874`; `academy-watch-backend/src/services/gol_credits.py:199`; `CONTINUITY.md:104`. |
| **5.6 Adoption instrumentation** | 3 / A | 3 / A | **CONFIRMED** | An admin event summary is a legitimate admin-targeted feature. The event vocabulary is broader than the old six-event evidence, but there is no complete cross-client, non-team activation/retention funnel. A checkout-return browser event is not financial truth. `academy-watch-backend/src/routes/events.py:24`; `academy-watch-backend/src/routes/events.py:144`; `academy-watch-frontend/src/pages/admin/AdminDashboard.jsx:106`; `academy-watch-frontend/src/pages/AccountBillingPage.jsx:99`. |
| **5.7 Legal and paper** | 2 / WI | 2 / partial W; iOS release unconfirmed | **CONFIRMED grade; current copy mismatches paid product** | Legal pages exist, but the billing-enabled text describes auto-renewing subscriptions while the launched offer is intended to be prepaid GOL credits. The privacy inventory does not fully describe the credit ledger. Upload/funding acceptance is not mechanically complete. `academy-watch-frontend/src/pages/LegalPages.jsx:99`; `academy-watch-frontend/src/pages/LegalPages.jsx:145`; `academy-watch-backend/src/routes/club.py:1253`. |
| **5.8 Real adoption** | 1 / WI | 1 / historical evidence only | **CONFIRMED provisionally; reach label is not proof** | This cannot be verified from routes, fixtures or seeded accounts. The available measured baseline reports negligible participation. I retain 1 rather than fabricate a current count or treat shipped onboarding as adoption. `ledgers/GRADING_dream-scorecard-2026-09-02.md:11`; `ledgers/GRADING_dream-scorecard-2026-09-02.md:97`. |

The numerical changes are therefore limited and explicit:

| Pillar | Published | Reviewed |
|---|---:|---:|
| P1 | 75.0% | 75.0% |
| P2 | 63.9% | 66.7% |
| P3 | 75.0% | 71.4% |
| P4 | 46.4% | 46.4% |
| P5 | 59.4% | 56.2% |
| **Weighted overall** | **65.6%** | **65.1%** |

These figures use the tool’s per-pillar rounding before weighted aggregation, rather than a separately invented formula. (`ledgers/tooling/dream-scorecard/score.py:58`)

## Part 2 — Defects

These are source-grounded failure scenarios, not claims that the failures have already occurred in production. **P1** means fix before exposing the affected money/trust path to customers; **P2** means a material correctness, usability or reach defect.

| Severity | Area | File:line | Failure scenario | Smallest correct fix |
|---|---|---|---|---|
| **P1** | GOL credit bypass / repeated model cost | `academy-watch-backend/src/services/gol_credits.py:99`; `academy-watch-backend/src/routes/gol.py:74`; `academy-watch-backend/src/routes/gol.py:123` | Reuse a successful `client_msg_id` and matching message after the balance reaches zero. The reservation returns `debited=False`, but the route starts another model execution. Worse, the hash excludes history/session, so the caller can change effective input while retaining the same charged message. Idempotent debit is not idempotent fulfillment. | Persist execution state and the completed response against the reservation. Return/replay that result, or reject an in-flight duplicate. Hash all accepted inputs affecting execution. Tie compensation to the exact debit attempt rather than resolving “latest” by message ID. |
| **P1** | Refund delivered before credit grant | `academy-watch-backend/src/services/stripe_billing.py:789`; `academy-watch-backend/src/services/stripe_billing.py:887`; `academy-watch-backend/src/services/stripe_billing.py:935` | `charge.refunded` arrives before the checkout grant. No grant is found, so the event becomes permanently `ignored`. Checkout later grants the full credits; replaying the refund returns duplicate success without reversing them. | Preserve unmatched refund state for reconciliation, or retrieve authoritative payment/refund state when granting. Apply grant plus cumulative reversal under the existing user lock. Test both event orders, concurrent delivery, partial refunds and duplicates. |
| **P1** | Unrelated club can create public “club-confirmed” statistics | `academy-watch-backend/src/routes/club.py:834`; `academy-watch-backend/src/routes/club.py:1037`; `academy-watch-backend/src/routes/club.py:1062`; `academy-watch-backend/src/services/season_rollup_service.py:822` | Roster attachment deliberately permits a tracked player without proving club affiliation because the roster is described as private. Result entry then turns that attachment into public club-confirmed statistics. For an eligible player without stronger provider cells, an unrelated approved club can publish misleading data and displace self-reported totals. | Keep unproven attachments private. Require an approved affiliation or player-authorized club relationship before a result becomes publicly club-confirmed. Reuse the existing claim/affiliation machinery; test a valid manager acting on an unrelated player. |
| **P2 — launch blocker** | GOL SSE parsing | `academy-watch-frontend/src/hooks/useGolChat.js:176`; `academy-watch-frontend/src/hooks/useGolChat.js:184` | A network chunk ends after `event: usage\n`; the next begins with its `data:` line. `eventType` resets to `token`, so usage, error or data-card events are misclassified. Users can see stale credit balances or lose retry/error behavior depending on transport chunking. | Keep SSE frame state outside the read loop and dispatch on the blank-line frame boundary. Test every split position, UTF-8 boundaries, multiple frames per chunk and terminal buffered data. |
| **P2 — launch blocker** | Mutable purchase entitlement | `academy-watch-backend/src/models/billing.py:77`; `academy-watch-backend/src/services/stripe_billing.py:237`; `academy-watch-backend/src/services/stripe_billing.py:702` | Checkout sells a pack, then configuration changes before its webhook arrives. Fulfillment reads the current pack’s credit quantity, not the purchased quantity. The customer receives a different amount—or a paid purchase remains unresolved if the pack disappears. | Snapshot price identity, credits, amount/currency expectations and product version in the checkout row. Fulfill from that snapshot, validating the signed event. Test configuration changes and delayed payment completion. |
| **P2** | Local-player merge loses references | `academy-watch-backend/src/routes/showcase.py:3899`; `academy-watch-backend/src/routes/showcase.py:3923`; `academy-watch-backend/src/services/player_subject.py:119` | Local→local merge moves claims/profile/media/affiliations/links/fans, then marks the source merged. It does not migrate the newly introduced match-entry references and other signed-ID consumers. Source statistics can disappear from the usable identity rather than following the player. | Extend this merge transaction using the existing rekey approach: enumerate subject-bearing tables, migrate/deduplicate references, refresh affected rollups, and verify both source and target routes afterward. Cover local→local separately from local→provider graduation. |
| **P2** | Club result correction and deletion | `academy-watch-backend/src/routes/club.py:1004`; `academy-watch-backend/src/routes/club.py:1041`; `academy-watch-backend/src/routes/club.py:1049`; `academy-watch-backend/src/routes/player_matches.py:457` | A manager corrects the date/opponent and creates another result identity; the old appearances remain. Omitting an erroneously included player from a resubmission does not remove that player’s old row. Owner deletion does not provide a club-result correction path. | Give the result a stable identity and manager-authorized correction/deletion semantics. Specify whether lineup updates replace or patch entries. Persist the optional video link. Refresh both old and new affected seasons after corrections. |
| **P2** | GOL payment completes after account deletion | `academy-watch-backend/src/services/stripe_billing.py:689`; `academy-watch-backend/src/services/stripe_billing.py:1123`; `academy-watch-backend/src/services/account.py:867` | Account deletion removes checkout/customer linkage. A delayed successful GOL payment then becomes `unresolvable_credit_purchase` and returns 500 repeatedly. The subscription-specific late-event fix does not solve the credit-purchase path. | Retain the minimum settlement/tombstone identifiers needed to acknowledge or refund late payments according to the chosen policy. Do not recreate the account. Test completed-but-not-granted purchases and delayed payment success after deletion. |
| **P2** | iOS excludes owned tracked players from game entry | `academy-watch-ios/AcademyWatch/Features/PlayerDetail/PlayerDetailView.swift:290`; `academy-watch-backend/src/routes/player_matches.py:231` | A claimant of a positive/provider player ID cannot add a game in iOS, although the backend authorizes owned positive and negative subjects. The native condition assumes provider subjects must be club-managed. | Gate on the backend-supported ownership/capability rule rather than ID sign. Add native list/edit/delete behavior for the owner journey and test both identity types. |
| **P2** | iOS stale season refresh | `academy-watch-ios/AcademyWatch/Features/PlayerDetail/PlayerDetailViewModel.swift:99`; `academy-watch-ios/AcademyWatch/Features/PlayerDetail/PlayerDetailViewModel.swift:167` | Saving a game starts an unversioned refresh. The user changes season while it is awaiting responses; the older refresh can overwrite the statistics/fixtures beneath the newly selected season. Normal loading already has revision protection, but this path bypasses it. | Route the refresh through the existing revision-controlled loader, or verify request revision and selected season before publishing. Test deliberately reordered responses and surface refresh failure. |
| **P2 — launch blocker** | Billing terms describe the wrong purchase | `academy-watch-frontend/src/pages/LegalPages.jsx:6`; `academy-watch-frontend/src/pages/LegalPages.jsx:99`; `academy-watch-frontend/src/pages/LegalPages.jsx:145` | Enabling the billing copy exposes subscription/auto-renewal language for a prepaid-credit offer and an incomplete data inventory. Flipping the flag alone therefore produces a customer-facing contradiction. | Replace the conditional copy with owner-approved prepaid-credit terms, refund/account-deletion treatment and actual data disclosures; bind acceptance to the relevant version. |
| **P2** | Sitemap silently excludes later players | `academy-watch-backend/src/services/sitemap_service.py:25`; `academy-watch-backend/src/services/sitemap_service.py:90`; `academy-watch-backend/src/services/sitemap_service.py:174` | Every build selects the same capped, ID-ordered candidate set, then applies public-adult filtering. Later eligible players never enter the sitemap; rejected candidates can consume the initial window. | Page through eligible candidates and produce a sitemap index/chunks as needed. Test beyond the current cap, including ineligible early IDs. |
| **P2** | Activity-email starvation | `academy-watch-backend/src/jobs/run_profile_activity_notifications.py:76`; `academy-watch-backend/src/services/profile_activity_notification_service.py:144`; `academy-watch-backend/src/services/profile_activity_notification_service.py:252` | Each run starts at the beginning. If early-ID users continue to generate enough sendable activity to exhaust the budget, later users can repeatedly miss their weekly notification. | Persist a fair cursor across runs or order by oldest successful notification with a stable tie-breaker. Test repeated budget-limited runs, not just pagination within one run. |

**What I would retain from the recent work.** The financial code has useful foundations: raw-body signature verification, unique webhook claiming with failed-event retry, user-row serialization for question credits, and cumulative refund accounting once the purchase grant exists. The video-credit race fix also uses explicit locking. The failures above are gaps around these mechanisms, not a reason to replace them. (`academy-watch-backend/src/services/stripe_billing.py:874`; `academy-watch-backend/src/services/stripe_billing.py:894`; `academy-watch-backend/src/services/gol_credits.py:98`; `academy-watch-backend/src/services/gol_credits.py:254`; `academy-watch-backend/src/routes/video.py:305`; `academy-watch-backend/src/routes/video.py:764`)

**Entitlements.** Scout verification remains distinct from paid status, which is correct. The paid GOL path uses credits; old subscription state and the `scout_tier` projection should not be mistaken for authorization to contact players or a reason to put provider-data exports behind the new purchase. With billing disabled, credit reservation intentionally bypasses charging, so “three free questions” is not the currently enforced dark-mode limit. (`academy-watch-backend/src/services/trust.py:14`; `academy-watch-backend/src/services/scout_entitlements.py:107`; `academy-watch-backend/src/services/gol_credits.py:83`)

**Migrations and RLS.** I found RLS-enabling statements and guarded creation in the inspected new match-entry, fan, billing, program-update and GOL migrations. A pure-local migration-graph inspection found one head, `s3d1`. This is positive source evidence, not proof of deployed RLS or a successful upgrade over a drifted database. In particular, the local-identity uniqueness migration can skip its index when duplicates exist; operators must inspect the resulting schema. (`academy-watch-backend/migrations/versions/pm01_player_match_entries.py:171`; `academy-watch-backend/migrations/versions/pm01_player_match_entries.py:192`; `academy-watch-backend/migrations/versions/s2f1_fans_reach.py:81`; `academy-watch-backend/migrations/versions/s3b1_billing_foundation.py:24`; `academy-watch-backend/migrations/versions/s3c1_club_program_editing.py:81`; `academy-watch-backend/migrations/versions/s3d1_gol_credit_ledger.py:24`)

**Season rollups.** The shared grain is real. Reported cells feed the same rollup, with provider→club→user source precedence rather than blindly adding overlapping sources. Preserve that rule and expose it clearly: a self-entered result may legitimately not change the displayed total when a stronger source supplies the selected cells. (`academy-watch-backend/src/services/season_rollup_service.py:782`; `academy-watch-backend/src/services/season_rollup_service.py:822`)

**iOS decoders and walks.** The null-name fix is aligned with the captured JSON: the decoder accepts absent/null names, and the backend enriches club metadata. Fixture tests cover the regression. The live UI walks are useful but narrow: search/detail, reviewer sign-in, watchlist and list mutations. They do not prove claims, game mutations, fan controls, consent, billing or general mailbox sign-in. The reviewer walk skips without its required credentials, and the dedicated smoke scheme must actually be run. (`academy-watch-ios/AcademyWatch/Core/Models/PlayerDetailModels.swift:250`; `academy-watch-backend/src/routes/players.py:324`; `academy-watch-ios/AcademyWatchTests/SeasonDecodingTests.swift:51`; `academy-watch-ios/AcademyWatchUITests/SmokeUITests.swift:114`; `academy-watch-ios/AcademyWatchUITests/README.md:3`)

## Part 3 — Plan to 100

Effort estimates are engineering effort, including meaningful tests and review: **S = up to 2 codex-days; M = 3–5; L = 6–15**. External approvals, recruiting, observation periods and legal work are additional.

The ordering favors revenue and completed user journeys, then score gain per effort. A useful scoring fact: a one-grade improvement is worth more in P1/P2 than in P5 because the criteria divide different pillar weights. All gains below were calculated from the existing formula. They are **isolated changes from 65.1%, not additive rounded promises**. (`ledgers/tooling/dream-scorecard/score.py:5`; `ledgers/tooling/dream-scorecard/score.py:58`)

A pull request alone earns none of the proposed 4s. The checker must verify deployment, the target user’s complete journey, relevant clients and the stated caveats. Dependencies on owner actions or real participants are called out explicitly.

### A. Engineering work that can earn gains without market adoption

| Rank | Move and criteria | Concrete work / required reach | Effort and risk | Evidence required |
|---|---|---|---|---|
| **A1** | **Make GOL safe to sell.** 3.6 **2→3**, 5.5 **2→3** after B2; **+1.2 points together**. Code fixes alone leave both at 2 while dark. | Fix replay/fulfillment, refund ordering, immutable purchase snapshots, deletion settlement and SSE parsing in the existing GOL/billing services and hook. Use `/api/billing/...`, `/api/gol/chat` and Account Billing. Launch at **W** first; do not require a native payment project for the first revenue. Existing seams: `academy-watch-backend/src/services/gol_credits.py:83`; `academy-watch-backend/src/services/stripe_billing.py:689`; `academy-watch-frontend/src/hooks/useGolChat.js:176`. | **M–L, 5–7 days.** Highest cost/revenue risk. Requires PostgreSQL concurrency tests, not only mocked sequential calls. | Same purchase fulfilled once across duplicate/out-of-order events; refund-before-grant ends with correct balance; changed pack config does not alter a sold entitlement; completed replay does not invoke the model; all SSE chunk boundaries work. Then B2 live acceptance. |
| **A2** | **Finish permissioned player feedback.** 2.7 **2→4**, **+1.4 points**. | Extend existing roster briefs with explicit player-visible feedback/history and recipient authorization. Add manager delivery controls and player inbox/detail views on **W and iOS**. Keep private coach/system prompts private. `academy-watch-backend/src/routes/club.py:879`; `academy-watch-frontend/src/pages/MyClubConsole.jsx:549`; `academy-watch-frontend/src/components/video/PlayerReel.jsx:158`. | **M, 3–5 days.** Main risk is exposing private coaching material or granting access after a relationship ends. | Manager creates feedback; correct claimed player reads it on both clients; unrelated player/manager cannot; revoked membership behavior is specified and tested; old feedback remains attributable rather than silently overwritten. |
| **A3** | **Complete the owner’s profile and games journey.** 1.1–1.3 **3→4**, **+2.3 points**. | Finish native bio/media/claim editing and match list/create/edit/delete for both ID types. Preserve the last approved public profile while staging sensitive changes. Fix local merge rekeying and native refresh ordering. **WI**, after B1. `academy-watch-backend/src/routes/showcase.py:2795`; `academy-watch-backend/src/routes/showcase.py:3899`; `academy-watch-backend/src/routes/player_matches.py:339`; `academy-watch-ios/AcademyWatch/Features/PlayerDetail/PlayerDetailView.swift:290`. | **L, 6–9 days.** Identity migration and moderation-state mistakes can lose data or publish unapproved claims. | Fresh local and tracked owners complete the full journey on both clients; edits survive relaunch; pending sensitive edits do not erase the approved public version; merged identities retain games and relationships; cross-season responses cannot overwrite the selected season. |
| **A4** | **Make discovery and growth consistently explainable.** 1.4, 1.8, 3.2, 3.3 **3→4**, **+3.0 points**. | Finish signed-subject parity in filters, comparison and native navigation; explain provider/club/self source selection; expose only consented, sanitized analysis evidence to scouts. Build on the existing scout union, rollups and verified-footage projection. **WI**. `academy-watch-backend/src/routes/scout.py:431`; `academy-watch-backend/src/routes/scout.py:1354`; `academy-watch-backend/src/services/season_rollup_service.py:822`; `academy-watch-backend/src/routes/showcase.py:1306`. | **L, 6–10 days.** Privacy and misleading statistical comparisons are the risks; do not invent totals to fill gaps. | The same local/provider subjects are findable and comparable on both clients; provenance survives every presentation; overlapping sources do not double-count; private footage and unapproved subjects cannot leak through reports or exports. |
| **A5** | **Finish trustworthy club operations.** 2.1–2.3 and 4.7 **3→4**, **+2.6 points**. | Add player-owned profile invitations/acceptance; separate private roster attachment from public club authority; add stable result correction/deletion, persisted video association, entry attestation and disputes. Provide the core manager journeys on relevant clients. **W plus native manager essentials**. `academy-watch-backend/src/routes/club.py:821`; `academy-watch-backend/src/routes/club.py:936`; `academy-watch-backend/src/services/club_registry.py:138`. | **L, 7–12 days.** Public attribution and fixture corrections cross authorization and rollup boundaries. | Player-created profile accepts a club invite; unrelated clubs cannot publish its confirmed statistics; typo correction removes old totals; lineup removal works; concurrent corrections preserve constraints; native manager can complete the routine workflow without admin operation. |
| **A6** | **Finish contact and recruitment authority.** 1.5, 2.9, 3.1, 3.5, 3.7 **3→4**, **+3.6 points**. | Add explicit adult/credential evidence to scout review; bind outreach to an authorized acting club when applicable; implement native club-consent inbox/decisions; complete report/block/revocation coverage across the conversation lifecycle. **WI**. `academy-watch-backend/src/routes/trust.py:301`; `academy-watch-backend/src/routes/contact.py:478`; `academy-watch-backend/src/routes/contact.py:934`; `academy-watch-ios/AcademyWatch/Core/Models/FullCircleModels.swift:84`. | **L, 6–10 days**, plus B3 policy decisions. Incorrect authority or revocation handling is the principal risk. | Direct, club-notified and club-consent journeys complete on both clients; unverified paid accounts cannot contact; revoked managers cannot act for a club; blocking/reporting works throughout; required subject eligibility is rechecked at the consequential transitions. |
| **A7** | **Put existing analytics inside MyClub.** 2.4, 2.5 **3→4**; 2.8 **2→4**. | Add manager-scoped tracked-player/pathway and roster-growth reads using `ClubProgram.team_api_id`, roster links and current rollups. Render a MyClub dashboard with comparison, coverage and actionable gaps; include community data, not only provider cohorts. **W and relevant native manager views**. Existing public path: `academy-watch-backend/src/routes/api.py:12853`; current console tabs: `academy-watch-frontend/src/pages/MyClubConsole.jsx:2099`. | **L, 6–9 days.** The danger is attractive charts with incompatible cohorts, missing coverage or unsupported conclusions. | A covered club and an uncovered grassroots club both get correct scoped views; totals reconcile with player pages; missing data is explicit; another club’s private roster is inaccessible. |
| **A8** | **Complete fans, notifications, discovery links and measurement.** 1.6, 1.7, 3.4, 5.3, 5.6 **3→4**, **+3.2 points**. | Bring native owner signals to web parity; distinguish fans/views/verified-scout interest; fix sitemap coverage and fair notification scheduling; complete supported notification preferences and delivery observability; add native funnel events and server-derived activation counts. **WI**, with **A** analytics. `academy-watch-backend/src/routes/showcase.py:1996`; `academy-watch-backend/src/services/sitemap_service.py:90`; `academy-watch-backend/src/services/profile_activity_notification_service.py:144`; `academy-watch-backend/src/routes/events.py:144`. | **L, 7–12 days.** Privacy, deliverability and counting the team as customers are the risks. | More than the sitemap cap is covered; repeated small-budget jobs eventually serve all eligible users; fan actions work across identities/clients; verified-scout counts exclude ordinary visitors; metrics distinguish test/team activity and browser return events from confirmed purchases. |
| **A9** | **Bind safety and corrections to the actual actions.** 5.1, 5.7 **2→4**; 5.2 **3→4**, **+2.4 points**, contingent on B3. | Apply consistent subject eligibility to claim/contact/media paths; require versioned rights/consent attestations; connect a resolved data flag to the correction event and reporter notification; expose necessary controls natively. The original dream’s U18 expansion needs an explicit supported policy, not accidental admission. **WI**. `academy-watch-backend/src/routes/showcase.py:1566`; `academy-watch-backend/src/routes/club.py:1253`; `academy-watch-backend/src/routes/api.py:6714`; `ledgers/GRADING_dream-scorecard-2026-09-02.md:123`. | **L, 8–15 days**, excluding policy/legal work. Scope depends heavily on the selected youth/guardian model. | A written eligibility matrix maps to negative and positive tests; unsafe uploads cannot publish; takedown and deletion propagate; each resolved dispute has a traceable correction or explicit rejection; acceptance records identify the applicable policy version. |
| **A10** | **Make Film Room genuinely self-service and operable.** 2.6, 5.4 **2→4**. | Have the existing club processing action enqueue through the existing job/credit mechanism. Add manager-scoped identity review/finalization, a usable allowance, resumable processing/checkpoints, managed worker deployment and queue/failure alerts. **W** for club operation, with reports consumable on relevant player clients. `academy-watch-backend/src/routes/club.py:1455`; `academy-watch-backend/src/services/video_queue.py:83`; `academy-watch-backend/src/workers/vision_worker.py:522`. | **L, 10–15 days**, plus B3/B4 decisions on rights and cost limits. GPU cost and identity mistakes dominate. | A normal approved manager uploads→queues→reviews→finalizes→shares without admin operation; worker death resumes safely; duplicate requests do not double-charge; failed processing has defined credit treatment; queue age and failure alerts fire in a controlled test. |

**Scoring maintenance, S effort, no product points:** regenerate evidence from this revision, repair contradictory reach labels, add consistency checks for dark grade-3 rows, and replace stale stage projections with explicit target-grade maps. The current report builder starts projections from today’s scores and applies old target maxima; it is not a historical S0→S6 progression or a complete 100% plan. (`ledgers/tooling/dream-scorecard/build_report.py:39`; `ledgers/tooling/dream-scorecard/build_report.py:88`; `ledgers/GRADING_dream-scorecard-2026-09-02.md:115`)

### B. Owner-action gains and owner-dependent product work

These are not “tell Codex to finish it” tasks. Each needs an explicit operating decision or external activation.

| Rank | Owner action / criteria | Exactly what must happen | Effort, risk and reach | Checker evidence |
|---|---|---|---|---|
| **B1** | **Make the repaired iOS build public.** Restores evidence for all W† rows; no automatic numeric increase. | Check App Store review/release state, resolve any rejection, release the repaired version, and verify installation from the public store. Test ordinary email-code sign-in as well as the reviewer path. The submitted-build record and fixed host are the starting points. `CONTINUITY.md:100`; `academy-watch-ios/AcademyWatch/Core/Networking/APIClient.swift:187`. | **S engineering**, external review wait unknown. Required reach: actual public **iOS**, not only a developer-installed build. | Store-installed version/build recorded; anonymous player/season read succeeds; normal user signs in; both identity types navigate correctly. A simulator walk against production alone does not establish public release. |
| **B2** | **Launch the prepaid GOL offer after A1.** 3.6 and 5.5 **2→3**. | Confirm the starter price and credit quantity against model cost; configure live one-time Stripe prices and the intended pack catalog; register the signed webhook and required checkout/refund events; configure credentials through the existing secret system; publish corrected billing copy; then enable the backend rail. Keep subscription products unoffered unless deliberately launching them. `academy-watch-backend/src/config/stripe_config.py:34`; `academy-watch-backend/src/config/stripe_config.py:61`; `academy-watch-backend/src/services/stripe_billing.py:738`; `academy-watch-frontend/src/pages/LegalPages.jsx:99`. | **S owner setup** after A1, plus live acceptance. Initial reach **W**. Risk: charging money against unproven settlement or misleading terms. | A real-card controlled purchase, verified server-side grant, consumption, refund, replay and account-state check. Record reconciliation, not card/customer secrets. Controlled acceptance proves the rail; it does not count as customer traction. |
| **B3** | **Approve the actual legal/data/safety posture.** Enables 5.1 and 5.7 **2→4**, and removes constraints on 3.3/3.6/4.2/4.5. | Approve prepaid-credit refund/expiry/deletion treatment; update Privacy to actual credit, event and model-processing data; establish upload rights and age/guardian rules; confirm commercial permissions for the data used in paid GOL answers. For funding and ownership, obtain jurisdiction-specific advice and record the selected structure before implementation. Do not assume generated prose automatically resolves source-data restrictions. Existing mismatches: `academy-watch-frontend/src/pages/LegalPages.jsx:99`; `academy-watch-frontend/src/pages/LegalPages.jsx:145`; owner’s paid-product rationale: `ledgers/GRADING_dream-scorecard-2026-09-02.md:71`. | **S–M integration**, external work unestimated. Reach: policy and enforcement across **WI** and relevant admin operations. | Approved policy versions, a data inventory, documented permissions/decisions, and tests proving the policies are enforced at upload, purchase, contact and deletion. |
| **B4** | **Choose and enable real club support.** 4.1/4.4/4.6 **3→4** only with C evidence; 4.2 **1→3**; 4.3 **0→3** for launched technical completion. | Select a real club and jurisdiction. Decide recipient onboarding, fees, tips, refund responsibility, supporter visibility and recurring tiers. First finish program discovery and the moderated external-platform connection, including consented supporter import if that is the agreed complete connection. Then build native club-support checkout/settlement/receipts/refunds and recurring lifecycle in the existing funding/billing areas. Do not flip `is_fundable` before those mechanisms exist. `academy-watch-backend/src/models/funding.py:272`; `academy-watch-backend/src/routes/funding.py:1234`; `academy-watch-frontend/src/pages/ProgramPage.jsx:164`. | **L, 10–15 days for initial native money movement; another M–L for recurring/supporter administration**, after owner decisions. Reach: public **W**, plus relevant native club/supporter journeys for 4s. | A discoverable approved program; authorized recipient; reconciled payment and refund; supporter receipt; cancellation and renewal handling; program updates visible to the correct audience. Genuine supporter evidence remains C. |
| **B5** | **Resolve whether ownership is truly in scope.** 4.5 **0→3** for a legally supported, operational mechanism; **4 requires C**. | Choose an actual permitted ownership/governance vehicle with qualified advice. Then implement the minimum club-linked allocation/register, transaction records, disclosures, participant statements and governance workflow that vehicle requires. If the owner instead chooses ordinary membership perks, explicitly revise the dream; do not award the existing ownership criterion. Current scorecard has no implemented mechanism. `ledgers/GRADING_dream-scorecard-2026-09-02.md:82`; `ledgers/GRADING_dream-scorecard-2026-09-02.md:120`. | **Multiple L packages**, only estimable after scoping; external work dominates. Reach: participant-facing product plus a usable administrative register. | The governing documents and product behavior agree; holdings and transactions reconcile; required eligibility/disclosures are enforced; real issuance/participation is evidenced before grade 4. |

For A10, the owner must also choose the Film Room allowance and acceptable operating cost. The existing lifetime quota is a product restriction, not merely a missing button; bypassing it without a cost decision is not completion. (`academy-watch-backend/src/routes/club.py:664`; `academy-watch-backend/src/routes/club.py:1174`)

### C. Gains that cannot be earned by code

| Rank | Real-world milestone | Criteria and cap | What the product must make easy | Evidence required |
|---|---|---|---|---|
| **C1** | **One real, coherent cohort completes the loop.** Recruit one club, its manager, adult players, a scout and supporters/fans; aim for at least ten distinct non-team participants. | **5.8: 1→3**, then **3→4** only with an agreed sustained-use standard. The stage plan’s ten-participant milestone is not automatically full adoption. `ledgers/GRADING_dream-scorecard-2026-09-02.md:97`; `ledgers/GRADING_dream-scorecard-2026-09-02.md:121`. | Invitations, claim approval, profile completion, game entry, club association, feedback, watchlist and consented introduction should form one understandable journey. Fix missing buttons encountered by this cohort before expanding features. | Distinct non-team accounts with completed actions and a traceable end-to-end outcome; return use over an agreed observation period; interviews identifying why people came back. Seed data and the reviewer scout do not count. |
| **C2** | **Customers pay, consume value and return.** Include genuine GOL purchases/top-ups and genuine club-support activity. | For conservative planning, **3.6 and 5.5 cap at 3** without real paid use; **4.1–4.4 and 4.6 cap at 3** without real club/supporter operation. These caps make explicit the rubric’s existing “no real programs/supporters/payments” blockers. `ledgers/GRADING_dream-scorecard-2026-09-02.md:71`; `ledgers/GRADING_dream-scorecard-2026-09-02.md:78`; `ledgers/GRADING_dream-scorecard-2026-09-02.md:83`. | Transparent pricing and balances, reliable checkout, useful answers, clear receipts/refunds, program discovery, supporter updates and effortless cancellation. External support must be attributable with the participant’s consent. | Settled non-team revenue reconciled to fulfilled purchases; actual credit consumption; repeat purchase or retained usage; real recipient settlement; at least a real recurring renewal/cancellation lifecycle where recurring support is claimed. Owner acceptance charges are excluded from traction. |
| **C3** | **Real people hold the promised ownership interest.** | **4.5 caps at 3** for a complete but unused technical/legal mechanism. Missing legal structure or software keeps it lower. `ledgers/GRADING_dream-scorecard-2026-09-02.md:82`. | Accurate holdings, understandable rights, required disclosures, participant statements and usable governance actions. | Actual lawful issuance/allocation, reconciled consideration where applicable, participant records and exercised or demonstrably available rights. A “supporter” badge is insufficient. |

The acceptance standard for sustained adoption is currently underspecified. The owner should choose the observation period and qualifying actions **before** collecting the result, so the checker cannot move the goalposts afterward. The existing admin event summary provides a starting point, not this complete definition. (`academy-watch-backend/src/routes/events.py:144`; `ledgers/GRADING_dream-scorecard-2026-09-02.md:95`)

**The ceiling, explicitly calculated:**

- **Strict mathematical ceiling with only 5.8 left at 1:** set all other 38 criteria to 4 → **98.6%**.
- **My realistic A+B-complete/C-not-complete planning ceiling:** all other criteria at 4, but **3.6, 4.1, 4.2, 4.3, 4.4, 4.5, 4.6 and 5.5 remain at 3**, with **5.8 at 1** → **94.2%**.
- The second figure is an explicit reviewer acceptance assumption, not a hidden feature of `score.py`. It withholds “complete” from unproven payment, club-support and ownership operation. Both figures use the repository’s rounding and weights. (`ledgers/tooling/dream-scorecard/score.py:54`)
- **100% requires all 39 criteria at 4:** technical completion, relevant public clients, operational evidence, genuine adoption, genuine money movement, the intended youth-safety scope, and actual lawful ownership. Removing funding, ownership or U18 participation from the ambition may be a sensible business decision, but it changes the rubric rather than completing this one. (`ledgers/GRADING_dream-scorecard-2026-09-02.md:7`; `ledgers/GRADING_dream-scorecard-2026-09-02.md:21`; `ledgers/GRADING_dream-scorecard-2026-09-02.md:123`)

## Appendix

### Review boundary and local verification

Reviewed checkout: `fe7d25ff1175d042d7e75c1f0e2c5fdd68095fd8`. I inspected the recent S1–S3/iOS changes, load-bearing current code, migration declarations, relevant tests and captured iOS fixtures. I did not run the application, use the network, connect to production, install dependencies or edit files. The final working tree remained clean.

`score.py` writes `scorecard.json`, so an ordinary invocation would violate the read-only brief. I executed its actual code with only that write redirected to memory:

```sh
cd ledgers/tooling/dream-scorecard
python3 -c 'import builtins,io,runpy; original=builtins.open; builtins.open=lambda file,mode="r",*a,**k: io.StringIO() if mode=="w" and str(file).endswith("scorecard.json") else original(file,mode,*a,**k); runpy.run_path("score.py",run_name="__main__")'
```

It reproduced **65.6%**. Separate in-memory calculations applied the three review changes and the explicit ceiling maps, producing **65.1%, 94.2% and 98.6%**.

Additional pure-local probes:

- Extracted the actual reservation function into an isolated test namespace: a matching unreversed debit returned `debited=False` even with both balances exhausted.
- Exercised the actual event-dispatch branch with no matching purchase grant: the refund returned `False`; the webhook handler persists that as terminal `ignored`.
- Reproduced the hook’s parser logic with whole versus split event chunks: the same usage frame became `usage` in one case and `token` in the other.
- Parsed migration metadata and found one head.
- Inspected the captured season JSON and decoder regression tests.

These are focused reproductions and source checks, **not substitutes for the full backend, browser or Xcode suites**. Relevant implementations are at `academy-watch-backend/src/services/gol_credits.py:83`, `academy-watch-backend/src/services/stripe_billing.py:738`, `academy-watch-frontend/src/hooks/useGolChat.js:176`, and `academy-watch-ios/AcademyWatchTests/SeasonDecodingTests.swift:51`.

### What production access must settle

| Unverified item | Command/query or acceptance procedure |
|---|---|
| **Public iOS availability** | Run the ledger’s read-only App Store query: `asc.sh GET '/v1/apps/6799866798/appStoreVersions?fields[appStoreVersions]=versionString,appStoreState'`. Then verify the public-store installation, not just a locally signed build. Source of pending state: `CONTINUITY.md:100`. |
| **Actual deployed flags and frontend build configuration** | Have the operator compare deployed configuration against the intended settings privately, recording pass/fail rather than environment values. Authenticated `GET /api/billing/config` distinguishes a hidden rail from a configured offer; authenticated `GET /api/scout/entitlements` verifies the customer-facing entitlement response. Check local discovery, trusted edits, contact and season-read surfaces with representative accounts. Guard: `academy-watch-backend/src/routes/billing.py:38`. |
| **Migration head, RLS and indexes** | Query `SELECT version_num FROM alembic_version;`. Inspect `pg_class.relrowsecurity`, `pg_policies` and `pg_indexes` for the tables introduced by the inspected migrations. Compare index definitions—not just table existence—with migration declarations, especially the conditionally skipped uniqueness index. `academy-watch-backend/migrations/versions/pm01_player_match_entries.py:171`. |
| **Stripe fulfillment and financial state** | In a controlled acceptance environment, deliver checkout/refund events in both orders, duplicate them, race them, change pack configuration between checkout and fulfillment, and complete payment after account deletion. Reconcile Stripe payment/refund amounts to checkout and GOL ledger rows per currency. Do not infer payment from the browser’s return URL. `academy-watch-backend/src/services/stripe_billing.py:689`; `academy-watch-frontend/src/pages/AccountBillingPage.jsx:99`. |
| **Races under the actual database** | Run the relevant billing, GOL, result-entry and video-credit tests against PostgreSQL, adding the scenarios above. Two independent transactions must exercise each race; a sequential mocked test is insufficient evidence for lock behavior. Existing lock sites: `academy-watch-backend/src/services/gol_credits.py:98`; `academy-watch-backend/src/routes/club.py:972`; `academy-watch-backend/src/routes/video.py:305`. |
| **Season backfill and current JSON shapes** | Read `/api/admin/season-rollup/status`; compare season/profile/recent-fixture responses for a provider player, a local self-reporter and a club-confirmed subject. Include absent names, negative club IDs, missing statistics and overlapping sources. Feed captured responses through the native decoders. `academy-watch-backend/src/routes/players.py:324`; `academy-watch-backend/src/routes/players.py:868`; `academy-watch-ios/AcademyWatchTests/SeasonDecodingTests.swift:51`. |
| **Native end-to-end coverage** | Run the dedicated `AcademyWatchUISmoke` scheme using the command in `academy-watch-ios/AcademyWatchUITests/README.md:12`; record skipped tests as skipped. Add owned positive/negative game CRUD, fan controls, claims and consent walks as those journeys are completed. |
| **Managed jobs, workers and delivery** | Inspect deployed job definitions, active image revisions, worker processes, queue age, failed jobs and recent executions. Run a controlled ordinary-manager upload through completion and budget-limited notification runs across multiple invocations. An image-update loop that skips missing jobs is not evidence those jobs exist. `.github/workflows/deploy.yml:225`; `academy-watch-backend/src/routes/club.py:1455`. |
| **Adoption and retention** | Query non-team account activation, approved player/club claims, watchlists, accepted introductions, feedback use and paid ledger grants over the agreed period. Exclude designated team/test accounts and acceptance purchases. Use server records to corroborate browser events; obtain current counts rather than repeating September 2’s baseline. `ledgers/GRADING_dream-scorecard-2026-09-02.md:11`; `academy-watch-backend/src/routes/events.py:144`. |
| **Legal/data permissions and ownership feasibility** | Review the actual approved documents and permissions with the owner and qualified advisers. Repository placeholders cannot establish commercial rights, a valid ownership vehicle or a complete youth-consent model. `academy-watch-frontend/src/pages/LegalPages.jsx:99`; `ledgers/GRADING_dream-scorecard-2026-09-02.md:82`. |