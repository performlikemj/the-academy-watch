# Private media through the app — review fix 2 complete

- Owner decision A: containers stay private; no cloud/infrastructure access or migrations.
- Public `GET|HEAD /api/media/published/<path>` validates paths, matches a stored photo/current banner, then authorizes before storage or 304. Photos reuse `resolve_public_adult_subject` and anonymous `_local_player_visible_to_context`; minors, unknown ages, suppression, club provenance, hidden/merged identities and unapproved/deleted items fail closed. Banners require `emergency_hidden=false` and platform status `pending|approved`.
- Cache policy corrected to **`private, max-age=300, must-revalidate`** on GET/HEAD/304; CDN caching prohibited, browser freshness bounded to five minutes. ETag and nosniff retained. Limit **300/minute per client**, using `get_client_ip`: CF-Connecting-IP, then X-Forwarded-For, X-Real-IP, remote address.
- Approved private previews: `/api/showcase/media/<id>/preview` requires a current approved subject claim or active approved manager of the recorded origin program; suppression/invalid identities fail closed. `/api/admin/showcase/media/<id>/preview` uses existing dual admin auth. Both stream with `private, no-store`, never place bearer credentials in URLs, and reauthorize conditional requests. Deleted/non-approved items remain 404; public minor URLs always remain 404.
- `public_url` is null when a photo is not publicly eligible. Authorized owner/admin serializers add `approved_preview_url`; shared `ShowcasePhoto` fetches credentials only to validated app paths, renders object URLs and revokes them on cleanup. Existing manager-only club photo transport remains unchanged.
- Reused module-level Azure service client, creation guarded by a lock; request-local downloaders. Both SDK initial/chunk GET sizes are **1 MiB**, confirmed on actual child blob configuration without network access. Initial storage errors return rate-limited-warning 404; interrupted streams terminate incomplete without repeated tracebacks. Malformed stored references return null and warn at most every five minutes by category, preserving whole payloads.
- Publishing still stores paths in existing URL columns; wire URLs use PUBLIC_API_BASE_URL/public_api_origin. First-use container diagnostics never create containers or change access levels. Existing dev published-file bypass remains closed. iOS read-only; SWA/Vite impose no image CSP restriction.

Verification on Basecamp (logs/scratch `/tmp/media-proxy-work/`, `fix1-*`):
- Focused backend: **356 passed**, including **72 proxy cases** and auth, showcase/media/trust, club home/player/console coverage. New regressions include forwarded-client bucket isolation, GET/HEAD/304 cache policy, club takedown, invalid references, SDK read/chunk errors, claimant/admin/manager preview authorization and revocation.
- Full backend: **2975 passed, 35 skipped, 3 failed** (only known radar full-chart, percentile-pool and old/new-comparison failures), in UTC with dummy integration credentials against clone `media_proxy_fix1_20260925`. Prior unchanged `origin/main` 6db39d8 reproduction: identical three radar failures, two passed, one skipped; baseline transfer tests pass in UTC.
- Ruff check and format: pass, **513 files**. Frontend lint: **0 errors, 182 existing warnings**; build passes (existing bundle-size warning); Node tests **189 passed**, including three preview credential-boundary tests. No separate JS/JSX typecheck command. No dependency or lockfile changes; frozen dependencies from previous OSV-clean restore reused.
- Playwright: **3 passed** against throwaway Postgres: real upload/admin approval/anonymous photo + HTTP HEAD/304; banner upload/replacement; approved minor rendering for claimant/admin, with anonymous/stranger/public denials. No API mocks; external browser requests blocked. Seven screenshots in `shots/`, including `06-minor-owner-private.png` and `07-minor-admin-private.png`.
- Clone restored from the prior read-only local dump and migrated/seeded only in isolation. Backend/frontend listeners stopped; clone dropped and absence verified. Seven screenshots retained; minor owner/admin screenshots visually inspected. One new commit after c3dc16d; no amend. Root CONTINUITY.md untouched per file fence; no .github/iOS/lockfile changes, push, PR or merge. Both `/tmp/media-proxy.log` and `/tmp/media-proxy-fix1.log` preserved.

Fix round 2:
- Public-prefix error responses (including unmatched paths, HEAD misses and limiter 429s) now carry `private, no-store` via an app response hook. Successful GET/HEAD/304 policy and visibility gates are unchanged.
- Subject visibility is memoized in the request environment, keyed by local/API identity; per-row lifecycle checks remain uncached. No decision survives a request. SQL-count regressions compare one versus thirteen photos for local/tracked and visible/hidden subjects, and verify revocation on the next request.
- Admin media keeps its `media` array, adding `total`, `limit`, `offset`. Default 50, hard maximum 100, stable created-at/id descending order, filtered totals. UI has Previous/Next, filter reset and last-page recovery after moderation.
- Storage warnings retain category-only messages and a lock; monotonic timestamps allow one warning per category every 300 seconds.
- Focused backend **387 passed**, including **84 proxy cases**. Ruff check/format pass (**513 files**). Frontend lint **0 errors / 182 existing warnings**, build pass, unit tests **189 passed**; added pagination browser spec also lint-clean.
- Playwright pagination **1 passed** with synthetic API responses and external requests blocked: previous/next, status reset, final-item moderation recovery. Real storage flows were verified in fix round 1; no new storage-byte behavior in this round.
- Full backend **2987 passed / 35 skipped / 3 known radar failures**, 191 seconds in UTC against `media_proxy_fix2_20260925`; identical named radar failures to the earlier origin/main baseline. No additional failures.
- Frontend listener stopped; throwaway DB dropped and absence verified (0 matches). Logs retained under `/tmp/media-proxy-work/fix2-*`; all earlier shots/logs and `/tmp/media-proxy-fix2.log` preserved. No production/Azure/Supabase access.
- Six changed files: showcase route, storage service, proxy tests, AdminShowcase UI, pagination browser test and this note. One new commit after e49aad4, exact requested message/footer; no amend/push. No root CONTINUITY.md, iOS, dependency/lockfile, or infrastructure edits. No separate JS/JSX typecheck command; production build passed.
